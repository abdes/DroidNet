<?xml version="1.0" encoding="utf-8" ?>
<UserControl
    x:Class="Oxygen.Editor.World.Output.OutputView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:dnc="using:DroidNet.Controls.OutputConsole"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Padding="4">
        <dnc:OutputConsoleView
            x:Name="OutputConsole"
            FollowTail="{x:Bind ViewModel.FollowTail, Mode=TwoWay}"
            IsPaused="{x:Bind ViewModel.IsPaused, Mode=TwoWay}"
            ItemsSource="{x:Bind ViewModel.Buffer, Mode=OneWay}"
            LevelFilter="{x:Bind ViewModel.LevelFilter, Mode=TwoWay}"
            ShowTimestamps="{x:Bind ViewModel.ShowTimestamps, Mode=TwoWay}"
            TextFilter="{x:Bind ViewModel.TextFilter, Mode=TwoWay}"
            WordWrap="{x:Bind ViewModel.WordWrap, Mode=TwoWay}" />
    </Grid>
</UserControl>
