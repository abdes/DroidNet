<?xml version="1.0" encoding="utf-8" ?>
<UserControl
    x:Class="Oxygen.Editor.WorldEditor.Editors.SceneEditorView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid
            MaxWidth="720"
            Padding="20"
            HorizontalAlignment="Center"
            VerticalAlignment="Center">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <!--  Header  -->
            <StackPanel Grid.Row="0" Spacing="6">
                <TextBlock Style="{ThemeResource TitleTextBlockStyle}" Text="Scene Editor" />
                <TextBlock Style="{ThemeResource SubtitleTextBlockStyle}" Text="{x:Bind ViewModel.Metadata.Title, Mode=OneWay}" />
            </StackPanel>

            <!--  Metadata details  -->
            <Border
                Grid.Row="1"
                Margin="0,12,0,12"
                Background="{ThemeResource SystemControlBackgroundBaseLowBrush}"
                CornerRadius="6">
                <Grid Padding="12">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="200" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>

                    <TextBlock
                        Grid.Row="0"
                        Grid.Column="0"
                        Style="{ThemeResource CaptionTextBlockStyle}"
                        Text="Document Type" />
                    <TextBlock
                        Grid.Row="0"
                        Grid.Column="1"
                        Style="{ThemeResource BodyTextBlockStyle}"
                        Text="{x:Bind ViewModel.Metadata.DocumentType, Mode=OneWay}" />

                    <TextBlock
                        Grid.Row="1"
                        Grid.Column="0"
                        Style="{ThemeResource CaptionTextBlockStyle}"
                        Text="Document Id" />
                    <TextBlock
                        Grid.Row="1"
                        Grid.Column="1"
                        Style="{ThemeResource BodyTextBlockStyle}"
                        Text="{x:Bind ViewModel.Metadata.DocumentId, Mode=OneWay}" />

                    <TextBlock
                        Grid.Row="2"
                        Grid.Column="0"
                        Style="{ThemeResource CaptionTextBlockStyle}"
                        Text="Is Dirty" />
                    <TextBlock
                        Grid.Row="2"
                        Grid.Column="1"
                        Style="{ThemeResource BodyTextBlockStyle}"
                        Text="{x:Bind ViewModel.Metadata.IsDirty, Mode=OneWay}" />

                    <TextBlock
                        Grid.Row="3"
                        Grid.Column="0"
                        Style="{ThemeResource CaptionTextBlockStyle}"
                        Text="Is Pinned" />
                    <TextBlock
                        Grid.Row="3"
                        Grid.Column="1"
                        Style="{ThemeResource BodyTextBlockStyle}"
                        Text="{x:Bind ViewModel.Metadata.IsPinnedHint, Mode=OneWay}" />

                    <TextBlock
                        Grid.Row="4"
                        Grid.Column="0"
                        Style="{ThemeResource CaptionTextBlockStyle}"
                        Text="Is Closable" />
                    <TextBlock
                        Grid.Row="4"
                        Grid.Column="1"
                        Style="{ThemeResource BodyTextBlockStyle}"
                        Text="{x:Bind ViewModel.Metadata.IsClosable, Mode=OneWay}" />

                    <TextBlock
                        Grid.Row="5"
                        Grid.Column="0"
                        Style="{ThemeResource CaptionTextBlockStyle}"
                        Text="Layout" />
                    <TextBlock
                        Grid.Row="5"
                        Grid.Column="1"
                        Style="{ThemeResource BodyTextBlockStyle}"
                        Text="{x:Bind ViewModel.Metadata.Layout, Mode=OneWay}" />
                </Grid>
            </Border>

            <!--  Placeholder for viewport/editor content  -->
            <Grid Grid.Row="2" Padding="12">
                <!--  Replace with actual viewport control  -->
            </Grid>
        </Grid>
    </Grid>
</UserControl>
