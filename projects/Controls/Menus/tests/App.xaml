<?xml version="1.0" encoding="utf-8" ?>

<tests:VisualUserInterfaceTestsApp
    x:Class="DroidNet.Controls.Tests.App"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:DroidNet.Controls.Tests"
    xmlns:tests="using:DroidNet.Tests">
    <Application.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <XamlControlsResources xmlns="using:Microsoft.UI.Xaml.Controls" />
                <!--
                    We are explicitly merging the controls Generic.xaml dictionary to get access
                    to its resources, so that we can define "forwarding" styles for the derived
                    testable controls.
                -->
                <ResourceDictionary Source="ms-appx:///DroidNet.Controls.Menus/Themes/Generic.xaml" />
            </ResourceDictionary.MergedDictionaries>
            <!--
                Forwarding styles for the testable controls, so that they pick up the same
                styling and control template as the original controls.
            -->
            <Style BasedOn="{StaticResource DefaultMenuItemStyle}" TargetType="local:TestableMenuItem" />
        </ResourceDictionary>
    </Application.Resources>
</tests:VisualUserInterfaceTestsApp>
