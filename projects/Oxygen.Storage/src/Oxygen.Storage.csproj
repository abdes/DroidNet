<?xml version="1.0" encoding="utf-8"?>

<Project Sdk="Microsoft.NET.Sdk">

    <PropertyGroup>
        <TargetFrameworks>net9.0-windows10.0.26100.0</TargetFrameworks>

        <Title>Oxygen Storage Layer</Title>
        <Description>Abstracts the storage of documents and entities in the Oxygen Editor and include implementation for various strage providers, including the native filesystem.</Description>
        <PackageReadmeFile>README.md</PackageReadmeFile>
        <PackageTags>oxygen;storage;filesystem</PackageTags>
        <PackageReleaseNotes>Initial version.</PackageReleaseNotes>
        <GeneratePackageOnBuild>True</GeneratePackageOnBuild>

        <RootNamespace>Oxygen.Storage</RootNamespace>
        <AssemblyName>$(MSBuildProjectName)</AssemblyName>
    </PropertyGroup>

    <ItemGroup>
        <InternalsVisibleTo Include="DroidNet.Oxygen.Storage.Tests" />
    </ItemGroup>

    <ItemGroup>
        <PackageReference Include="Testably.Abstractions" />
    </ItemGroup>

</Project>
