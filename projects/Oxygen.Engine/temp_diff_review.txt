diff --git a/projects/Oxygen.Engine/src/Oxygen/Graphics/Direct3D12/Shaders/Renderer/ShadowHelpers.hlsli b/projects/Oxygen.Engine/src/Oxygen/Graphics/Direct3D12/Shaders/Renderer/ShadowHelpers.hlsli
index 1d2bc178..cb43a9f7 100644
--- a/projects/Oxygen.Engine/src/Oxygen/Graphics/Direct3D12/Shaders/Renderer/ShadowHelpers.hlsli
+++ b/projects/Oxygen.Engine/src/Oxygen/Graphics/Direct3D12/Shaders/Renderer/ShadowHelpers.hlsli
@@ -559,8 +559,9 @@ static inline float SampleDirectionalVirtualShadowClipVisibility(
     const float3 biased_world_pos =
         world_pos + normal_ws * (metadata.normal_bias + renderer_normal_bias)
         + light_dir_ws * (metadata.constant_bias + renderer_constant_bias);
-    const float biased_light_view_depth =
-        mul(metadata.light_view, float4(biased_world_pos, 1.0)).z;
+    const float3 biased_light_view_pos =
+        mul(metadata.light_view, float4(biased_world_pos, 1.0)).xyz;
+    const float biased_light_view_depth = biased_light_view_pos.z;

     float2 page_coord = 0.0.xx;
     if (!ProjectDirectionalVirtualClip(metadata, clip_index, light_view_pos.xy, page_coord)) {
@@ -592,7 +593,12 @@ static inline float SampleDirectionalVirtualShadowClipVisibility(

     const uint tile_x = packed_entry & 0x0FFFu;
     const uint tile_y = (packed_entry >> 12u) & 0x0FFFu;
-    const float2 local_page_uv = saturate(float2(frac(page_coord.x), 1.0 - frac(page_coord.y)));
+    const float page_world_size = max(clip.origin_page_scale.z, 1.0e-4);
+    const float2 biased_page_coord =
+        (biased_light_view_pos.xy - clip.origin_page_scale.xy) / page_world_size;
+    const float2 local_page_uv = float2(
+        biased_page_coord.x - (float)page_x,
+        1.0 - (biased_page_coord.y - (float)page_y));

     uint pool_width = 0u;
     uint pool_height = 0u;
@@ -617,7 +623,7 @@ static inline float SampleDirectionalVirtualShadowClipVisibility(
         guard_texels / max((float)metadata.page_size_texels, 1.0);
     const float interior_span = max(1.0 - 2.0 * interior_min, 1.0e-4);
     const float2 atlas_uv = atlas_page_origin_uv
-        + (interior_min.xx + local_page_uv * interior_span) * page_extent_uv;
+        + (interior_min.xx + saturate(local_page_uv) * interior_span) * page_extent_uv;
     const float receiver_depth =
         biased_light_view_depth * clip.origin_page_scale.w + clip.bias_reserved.x;

diff --git a/projects/Oxygen.Engine/src/Oxygen/Renderer/Docs/implementation_plan.md b/projects/Oxygen.Engine/src/Oxygen/Renderer/Docs/implementation_plan.md
index f5b842a2..631e4295 100644
--- a/projects/Oxygen.Engine/src/Oxygen/Renderer/Docs/implementation_plan.md
+++ b/projects/Oxygen.Engine/src/Oxygen/Renderer/Docs/implementation_plan.md
@@ -704,14 +704,23 @@ Execution note, March 9, 2026:
     large flat receivers do not retain projected striping from under-biased
     virtual texels
   - virtual page lookup and local UV selection now stay on the un-biased
-    receiver position; only the depth comparison uses the biased receiver
-    position, preventing bias-driven page-row striping across large flat
-    receivers
+    receiver position was incorrect; page-table selection now stays on the
+    un-biased receiver, while local in-page sampling and depth comparison use
+    the biased receiver so normal-offset bias can shift the sample footprint
+    through the page guard texels without destabilizing page selection
+  - virtual directional clip levels no longer share one global light-space
+    depth mapping; each clip now tightens its own near/far range and publishes
+    its own depth scale/bias, so large flat receivers do not inherit striping
+    from wasted depth precision in the finest clip levels
   - virtual page-local sampling now flips `y` to match the rendered atlas tile
     orientation under D3D/Oxygen conventions
   - virtual clip selection now blends from a fine clip level into its next
     coarser neighbor near the outer clip boundary instead of hard-switching
     across nested clipmap regions
+  - receiver-driven virtual page selection now resolves to dilated contiguous
+    per-clip coverage rectangles under the current page budget instead of
+    sparse isolated page winners, so fine/coarse fallback does not print the
+    virtual page lattice into large receivers
   - runtime now logs one backend-selection line when a view switches between
     virtual, conventional, or no directional shadow backend so VSM activation
     can be verified without per-frame spam
diff --git a/projects/Oxygen.Engine/src/Oxygen/Renderer/Docs/shadows.md b/projects/Oxygen.Engine/src/Oxygen/Renderer/Docs/shadows.md
index d4207940..be3ebce3 100644
--- a/projects/Oxygen.Engine/src/Oxygen/Renderer/Docs/shadows.md
+++ b/projects/Oxygen.Engine/src/Oxygen/Renderer/Docs/shadows.md
@@ -968,6 +968,10 @@ Implementation status, March 9, 2026:
   - budget-bounded receiver-driven page selection from
     `PreparedSceneFrame.visible_receiver_bounding_spheres`, replacing the
     earlier centered-window-only approximation
+  - continuity-preserving receiver-driven coverage selection: per-clip virtual
+    requests now resolve to dilated contiguous page regions under budget
+    instead of sparse isolated page winners, so fine/coarse fallback does not
+    project the virtual page lattice onto large receivers
   - centered-window fallback only when receiver-driven selection requests no
     virtual pages for the view
   - deterministic per-clip prioritization and cross-frame resident-page reuse
@@ -983,9 +987,14 @@ Implementation status, March 9, 2026:
     frames by clearing only the pages it rerasterizes, not the entire atlas
   - virtual page raster and sampling must use page-local filter guard texels;
     page seams are not acceptable and cannot be papered over with post filtering
-  - virtual receiver bias must not perturb virtual page lookup or local page
-    UV selection; bias affects the depth comparison only, otherwise large flat
-    receivers inherit page-row striping from address drift
+  - virtual page-table selection must stay on the un-biased receiver position,
+    but local in-page sampling and depth comparison must follow the biased
+    receiver position so normal-offset bias can move the sample footprint
+    through guard texels without destabilizing page ownership
+  - virtual directional clips must publish clip-local depth scale/bias from
+    clip-local near/far fitting; one shared depth mapping across all clip
+    levels is not acceptable because it wastes depth precision and causes
+    receiver striping in the finest coverage
   - shader fallback from invalid fine virtual pages to valid coarser clip
     levels so partially resident windows remain continuous
 - Validation evidence:
diff --git a/projects/Oxygen.Engine/src/Oxygen/Renderer/Docs/virtual_shadow_map_backend.md b/projects/Oxygen.Engine/src/Oxygen/Renderer/Docs/virtual_shadow_map_backend.md
index ab69de5b..2fa250f1 100644
--- a/projects/Oxygen.Engine/src/Oxygen/Renderer/Docs/virtual_shadow_map_backend.md
+++ b/projects/Oxygen.Engine/src/Oxygen/Renderer/Docs/virtual_shadow_map_backend.md
@@ -311,8 +311,9 @@ Current intermediate behavior:
 - `VirtualShadowMapBackend` computes one camera-relative clipmap grid per level
 - visible receiver bounds from `PreparedSceneFrame` are projected into that
   grid to request pages per clip level
-- requested pages are prioritized by clip level, receiver overlap, and
-  proximity to the camera-relative clip center under the current page budget
+- requested pages now resolve to dilated contiguous per-clip coverage
+  rectangles under the current page budget instead of sparse isolated page
+  winners
 - the shader-visible page table is populated only for the selected resident
   pages
 - snapped-identical page tables and clip metadata reuse resident pages across
@@ -572,10 +573,15 @@ Current runtime note:
   after guard reservation; using the full physical page resolution
   underestimates virtual texel scale and leaves projected striping on large
   flat receivers
-- virtual page lookup and local UV selection must stay on the un-biased
-  receiver position; receiver bias affects the depth comparison only, not the
-  virtual page address, so large flat receivers do not inherit page-row
-  striping from bias-driven address drift
+- virtual page-table selection must stay on the un-biased receiver position so
+  page ownership is stable, but local in-page sampling and depth comparison
+  must use the biased receiver position so normal-offset bias can move the
+  sample footprint through the page guard texels without reintroducing page-row
+  striping
+- directional virtual clips must not share one global light-space depth range;
+  each clip level must tighten and publish its own near/far-derived depth
+  scale/bias so fine virtual clips retain adequate depth precision on large
+  flat receivers
 - invalid fine pages currently fall back to the next valid coarser clip in
   shading so bounded residency remains visually coherent
 - `virtual-only` remains the strict validation mode and does not use the
diff --git a/projects/Oxygen.Engine/src/Oxygen/Renderer/Internal/VirtualShadowMapBackend.cpp b/projects/Oxygen.Engine/src/Oxygen/Renderer/Internal/VirtualShadowMapBackend.cpp
index 1aadcc34..43f6df76 100644
--- a/projects/Oxygen.Engine/src/Oxygen/Renderer/Internal/VirtualShadowMapBackend.cpp
+++ b/projects/Oxygen.Engine/src/Oxygen/Renderer/Internal/VirtualShadowMapBackend.cpp
@@ -174,6 +174,103 @@ constexpr std::uint32_t kVirtualShadowMaxFilterGuardTexels = 2U;
   return std::clamp(resident_pages, 1U, pages_per_axis);
 }

+struct ClipCoverageRequest {
+  bool has_hits { false };
+  std::uint32_t min_page_x { 0U };
+  std::uint32_t max_page_x { 0U };
+  std::uint32_t min_page_y { 0U };
+  std::uint32_t max_page_y { 0U };
+  std::uint64_t total_hits { 0U };
+  double weighted_page_x { 0.0 };
+  double weighted_page_y { 0.0 };
+};
+
+[[nodiscard]] auto ResolveClipCoverageRectangle(
+  const ClipCoverageRequest& request, const std::uint32_t pages_per_axis,
+  const std::uint32_t budget_pages, const std::uint32_t dilation_pages)
+  -> std::optional<std::array<std::uint32_t, 4>>
+{
+  if (!request.has_hits || pages_per_axis == 0U || budget_pages == 0U) {
+    return std::nullopt;
+  }
+
+  const auto dilated_min_x = request.min_page_x > dilation_pages
+    ? request.min_page_x - dilation_pages
+    : 0U;
+  const auto dilated_min_y = request.min_page_y > dilation_pages
+    ? request.min_page_y - dilation_pages
+    : 0U;
+  const auto dilated_max_x
+    = std::min(pages_per_axis - 1U, request.max_page_x + dilation_pages);
+  const auto dilated_max_y
+    = std::min(pages_per_axis - 1U, request.max_page_y + dilation_pages);
+
+  const auto desired_width = dilated_max_x - dilated_min_x + 1U;
+  const auto desired_height = dilated_max_y - dilated_min_y + 1U;
+  const auto desired_area = desired_width * desired_height;
+  if (desired_area <= budget_pages) {
+    return std::array<std::uint32_t, 4> {
+      dilated_min_x,
+      dilated_max_x,
+      dilated_min_y,
+      dilated_max_y,
+    };
+  }
+
+  const double center_x = request.total_hits > 0U
+    ? request.weighted_page_x / static_cast<double>(request.total_hits)
+    : static_cast<double>(request.min_page_x + request.max_page_x) * 0.5;
+  const double center_y = request.total_hits > 0U
+    ? request.weighted_page_y / static_cast<double>(request.total_hits)
+    : static_cast<double>(request.min_page_y + request.max_page_y) * 0.5;
+
+  const double desired_aspect = static_cast<double>(desired_width)
+    / static_cast<double>(std::max(1U, desired_height));
+  std::uint32_t rect_width = std::clamp(
+    static_cast<std::uint32_t>(std::round(std::sqrt(
+      static_cast<double>(budget_pages) * std::max(desired_aspect, 1.0 / 16.0)))),
+    1U, pages_per_axis);
+  std::uint32_t rect_height = std::clamp(
+    std::max(1U, budget_pages / std::max(1U, rect_width)), 1U, pages_per_axis);
+
+  while (rect_width * rect_height > budget_pages) {
+    if (rect_width >= rect_height && rect_width > 1U) {
+      --rect_width;
+    } else if (rect_height > 1U) {
+      --rect_height;
+    } else {
+      break;
+    }
+  }
+
+  while ((rect_width + 1U) * rect_height <= budget_pages
+    && rect_width < pages_per_axis
+    && rect_width < desired_width) {
+    ++rect_width;
+  }
+  while (rect_width * (rect_height + 1U) <= budget_pages
+    && rect_height < pages_per_axis
+    && rect_height < desired_height) {
+    ++rect_height;
+  }
+
+  const auto max_origin_x = pages_per_axis - rect_width;
+  const auto max_origin_y = pages_per_axis - rect_height;
+  const auto origin_x = static_cast<std::uint32_t>(std::clamp(
+    std::floor(center_x - static_cast<double>(rect_width) * 0.5), 0.0,
+    static_cast<double>(max_origin_x)));
+  const auto origin_y = static_cast<std::uint32_t>(std::clamp(
+    std::floor(center_y - static_cast<double>(rect_height) * 0.5), 0.0,
+    static_cast<double>(max_origin_y)));
+
+  return std::array<std::uint32_t, 4> {
+    origin_x,
+    origin_x + rect_width - 1U,
+    origin_y,
+    origin_y + rect_height - 1U,
+  };
+}
+
 [[nodiscard]] auto SelectDirectionalVirtualFilterRadiusTexels(
   const float base_page_world, const float clip_page_world) -> std::uint32_t
 {
@@ -190,6 +287,34 @@ constexpr std::uint32_t kVirtualShadowMaxFilterGuardTexels = 2U;
   return (tile_x & 0x0FFFU) | ((tile_y & 0x0FFFU) << 12U) | kPageTableValidBit;
 }

+auto TightenDepthRangeWithShadowCastersForClipWindow(
+  const std::span<const glm::vec4> shadow_caster_bounds,
+  const glm::mat4& light_view, const float clip_center_x,
+  const float clip_center_y, const float ortho_half_extent_x,
+  const float ortho_half_extent_y, float& min_depth, float& max_depth) -> bool
+{
+  bool tightened = false;
+  for (const auto& sphere : shadow_caster_bounds) {
+    const float radius = sphere.w;
+    if (radius <= 0.0F) {
+      continue;
+    }
+
+    const glm::vec3 center_ls
+      = glm::vec3(light_view * glm::vec4(glm::vec3(sphere), 1.0F));
+    if (std::abs(center_ls.x - clip_center_x) > ortho_half_extent_x + radius
+      || std::abs(center_ls.y - clip_center_y) > ortho_half_extent_y + radius) {
+      continue;
+    }
+
+    min_depth = std::min(min_depth, center_ls.z - radius);
+    max_depth = std::max(max_depth, center_ls.z + radius);
+    tightened = true;
+  }
+
+  return tightened;
+}
+
 } // namespace

 namespace oxygen::renderer::internal {
@@ -681,8 +806,10 @@ auto VirtualShadowMapBackend::BuildDirectionalVirtualViewState(
   std::array<float, engine::kMaxVirtualDirectionalClipLevels> clip_page_world {};
   std::array<float, engine::kMaxVirtualDirectionalClipLevels> clip_origin_x {};
   std::array<float, engine::kMaxVirtualDirectionalClipLevels> clip_origin_y {};
-  std::array<float, engine::kMaxVirtualDirectionalClipLevels> camera_page_x {};
-  std::array<float, engine::kMaxVirtualDirectionalClipLevels> camera_page_y {};
+  std::array<float, engine::kMaxVirtualDirectionalClipLevels> clip_near_plane {};
+  std::array<float, engine::kMaxVirtualDirectionalClipLevels> clip_far_plane {};
+  std::array<float, engine::kMaxVirtualDirectionalClipLevels> clip_depth_scale {};
+  std::array<float, engine::kMaxVirtualDirectionalClipLevels> clip_depth_bias {};

   float prev_depth = std::max(near_depth, 0.0F);
   float largest_half_extent = 0.0F;
@@ -704,29 +831,6 @@ auto VirtualShadowMapBackend::BuildDirectionalVirtualViewState(
   const glm::vec3 camera_ls
     = glm::vec3(light_view * glm::vec4(camera_position, 1.0F));

-  float max_depth
-    = -(largest_half_extent + kLightPullbackPadding) + largest_half_extent;
-  float min_depth
-    = -(largest_half_extent + kLightPullbackPadding) - largest_half_extent;
-  [[maybe_unused]] const bool tightened = TightenDepthRangeWithShadowCasters(
-    shadow_caster_bounds, light_view, largest_half_extent, largest_half_extent,
-    min_depth, max_depth);
-
-  const float depth_padding
-    = std::max(kMinShadowDepthPadding, largest_half_extent * 0.1F);
-  const float near_plane = std::max(0.1F, -max_depth - depth_padding);
-  const float far_plane = std::max(near_plane + 1.0F, -min_depth + depth_padding);
-  const glm::mat4 depth_only_proj
-    = glm::orthoRH_ZO(-1.0F, 1.0F, -1.0F, 1.0F, near_plane, far_plane);
-  // Oxygen uses right-handed shadow views with forward along -Z, so derive the
-  // linear depth map from z=0 and z=-1 in light-view space rather than z=+1.
-  const float depth_at_zero
-    = TransformPoint(depth_only_proj, glm::vec3(0.0F, 0.0F, 0.0F)).z;
-  const float depth_at_negative_one
-    = TransformPoint(depth_only_proj, glm::vec3(0.0F, 0.0F, -1.0F)).z;
-  const float depth_scale = depth_at_zero - depth_at_negative_one;
-  const float depth_bias = depth_at_zero;
-
   const auto flags = BuildShadowProductFlags(candidate.light_flags);
   state.shadow_instances.push_back(engine::ShadowInstanceMetadata {
     .light_index = candidate.light_index,
@@ -762,16 +866,44 @@ auto VirtualShadowMapBackend::BuildDirectionalVirtualViewState(
     clip_origin_y[clip_index]
       = std::floor((camera_ls.y - half_extent) / page_world_size)
       * page_world_size;
-    camera_page_x[clip_index]
-      = (camera_ls.x - clip_origin_x[clip_index]) / page_world_size;
-    camera_page_y[clip_index]
-      = (camera_ls.y - clip_origin_y[clip_index]) / page_world_size;
     metadata.clip_metadata[clip_index].origin_page_scale
-      = glm::vec4(
-        clip_origin_x[clip_index], clip_origin_y[clip_index], page_world_size,
-        depth_scale);
-    metadata.clip_metadata[clip_index].bias_reserved
-      = glm::vec4(depth_bias, 0.0F, 0.0F, 0.0F);
+      = glm::vec4(clip_origin_x[clip_index], clip_origin_y[clip_index],
+        page_world_size, 1.0F);
+    metadata.clip_metadata[clip_index].bias_reserved = glm::vec4(0.0F);
+  }
+
+  for (std::uint32_t clip_index = 0U; clip_index < clip_level_count;
+    ++clip_index) {
+    const float half_extent = clip_half_extents[clip_index];
+    const float clip_center_x = clip_origin_x[clip_index] + half_extent;
+    const float clip_center_y = clip_origin_y[clip_index] + half_extent;
+
+    float min_depth = camera_ls.z - half_extent;
+    float max_depth = camera_ls.z + half_extent;
+    TightenDepthRangeWithShadowCastersForClipWindow(shadow_caster_bounds,
+      light_view, clip_center_x, clip_center_y, half_extent, half_extent,
+      min_depth, max_depth);
+
+    const float depth_padding
+      = std::max(kMinShadowDepthPadding, half_extent * 0.1F);
+    clip_near_plane[clip_index]
+      = std::max(0.1F, -max_depth - depth_padding);
+    clip_far_plane[clip_index] = std::max(
+      clip_near_plane[clip_index] + 1.0F, -min_depth + depth_padding);
+
+    const glm::mat4 depth_only_proj = glm::orthoRH_ZO(
+      -1.0F, 1.0F, -1.0F, 1.0F, clip_near_plane[clip_index],
+      clip_far_plane[clip_index]);
+    // GLM matrices are column-major: mat[col][row].
+    // Extract the exact Z scale/translation used by the orthographic projection
+    // instead of re-deriving them through transformed sample points.
+    clip_depth_scale[clip_index] = depth_only_proj[2][2];
+    clip_depth_bias[clip_index] = depth_only_proj[3][2];
+
+    metadata.clip_metadata[clip_index].origin_page_scale.w
+      = clip_depth_scale[clip_index];
+    metadata.clip_metadata[clip_index].bias_reserved.x
+      = clip_depth_bias[clip_index];
   }

   std::vector<std::uint16_t> receiver_hits(state.page_table_entries.size(), 0U);
@@ -848,108 +980,83 @@ auto VirtualShadowMapBackend::BuildDirectionalVirtualViewState(
     }
   }

-  struct RequestedPageCandidate {
-    std::uint32_t clip_index { 0U };
-    std::uint32_t page_x { 0U };
-    std::uint32_t page_y { 0U };
-    std::uint32_t local_page_index { 0U };
-    std::uint16_t hit_count { 0U };
-    float distance2_to_camera { 0.0F };
-  };
-
-  std::vector<std::vector<RequestedPageCandidate>> per_clip_candidates(
-    clip_level_count);
+  std::vector<ClipCoverageRequest> clip_requests(clip_level_count);
   for (std::uint32_t clip_index = 0U; clip_index < clip_level_count;
     ++clip_index) {
-    auto& clip_candidates = per_clip_candidates[clip_index];
-    clip_candidates.reserve(pages_per_level);
     for (std::uint32_t page_y = 0U; page_y < pages_per_axis; ++page_y) {
       for (std::uint32_t page_x = 0U; page_x < pages_per_axis; ++page_x) {
         const std::uint32_t local_page_index = page_y * pages_per_axis + page_x;
         const auto global_page_index = static_cast<std::size_t>(clip_index)
           * static_cast<std::size_t>(pages_per_level) + local_page_index;
-        if (receiver_hits[global_page_index] == 0U) {
+        const auto hit_count = receiver_hits[global_page_index];
+        if (hit_count == 0U) {
           continue;
         }
-
-        const float page_center_x = static_cast<float>(page_x) + 0.5F;
-        const float page_center_y = static_cast<float>(page_y) + 0.5F;
-        const float dx = page_center_x - camera_page_x[clip_index];
-        const float dy = page_center_y - camera_page_y[clip_index];
-        clip_candidates.push_back(RequestedPageCandidate {
-          .clip_index = clip_index,
-          .page_x = page_x,
-          .page_y = page_y,
-          .local_page_index = local_page_index,
-          .hit_count = receiver_hits[global_page_index],
-          .distance2_to_camera = dx * dx + dy * dy,
-        });
+        auto& request = clip_requests[clip_index];
+        if (!request.has_hits) {
+          request.has_hits = true;
+          request.min_page_x = page_x;
+          request.max_page_x = page_x;
+          request.min_page_y = page_y;
+          request.max_page_y = page_y;
+        } else {
+          request.min_page_x = std::min(request.min_page_x, page_x);
+          request.max_page_x = std::max(request.max_page_x, page_x);
+          request.min_page_y = std::min(request.min_page_y, page_y);
+          request.max_page_y = std::max(request.max_page_y, page_y);
+        }
+        request.total_hits += hit_count;
+        request.weighted_page_x += static_cast<double>(hit_count)
+          * (static_cast<double>(page_x) + 0.5);
+        request.weighted_page_y += static_cast<double>(hit_count)
+          * (static_cast<double>(page_y) + 0.5);
       }
     }
+  }

-    std::sort(clip_candidates.begin(), clip_candidates.end(),
-      [](const RequestedPageCandidate& lhs,
-        const RequestedPageCandidate& rhs) {
-        if (lhs.hit_count != rhs.hit_count) {
-          return lhs.hit_count > rhs.hit_count;
-        }
-        if (lhs.distance2_to_camera != rhs.distance2_to_camera) {
-          return lhs.distance2_to_camera < rhs.distance2_to_camera;
-        }
-        return lhs.local_page_index < rhs.local_page_index;
-      });
+  std::uint32_t pages_remaining = std::max(
+    1U, std::min(max_rendered_pages, static_cast<std::uint32_t>(selected_pages.size())));
+  std::uint32_t clips_with_hits = 0U;
+  for (const auto& request : clip_requests) {
+    if (request.has_hits) {
+      ++clips_with_hits;
+    }
   }

-  std::uint32_t pages_remaining
-    = std::max(1U, std::min(max_rendered_pages,
-      static_cast<std::uint32_t>(selected_pages.size())));
   for (std::uint32_t clip_index = 0U; clip_index < clip_level_count
        && pages_remaining > 0U;
        ++clip_index) {
-    const auto& clip_candidates = per_clip_candidates[clip_index];
-    if (clip_candidates.empty()) {
+    const auto& request = clip_requests[clip_index];
+    if (!request.has_hits) {
       continue;
     }
-    if (mark_selected_page(
-          clip_index, clip_candidates.front().page_x, clip_candidates.front().page_y)) {
-      --pages_remaining;
+
+    const std::uint32_t clips_left = std::max(1U, clips_with_hits);
+    const std::uint32_t reserve_for_rest = clips_left > 1U ? clips_left - 1U : 0U;
+    const std::uint32_t fair_share = std::max(1U, pages_remaining / clips_left);
+    const std::uint32_t clip_budget = std::max(
+      fair_share, pages_remaining > reserve_for_rest ? pages_remaining - reserve_for_rest : 1U);
+    const auto dilation_pages = std::max(1U,
+      SelectDirectionalVirtualFilterRadiusTexels(
+        clip_page_world[0], clip_page_world[clip_index]));
+    const auto selection = ResolveClipCoverageRectangle(
+      request, pages_per_axis, std::min(clip_budget, pages_remaining), dilation_pages);
+    if (!selection.has_value()) {
+      --clips_with_hits;
+      continue;
     }
-  }

-  while (pages_remaining > 0U) {
-    const RequestedPageCandidate* best_candidate = nullptr;
-    for (std::uint32_t clip_index = 0U; clip_index < clip_level_count;
-      ++clip_index) {
-      const auto& clip_candidates = per_clip_candidates[clip_index];
-      for (const auto& candidate_page : clip_candidates) {
-        const auto global_page_index = static_cast<std::size_t>(candidate_page.clip_index)
-          * static_cast<std::size_t>(pages_per_level)
-          + static_cast<std::size_t>(candidate_page.local_page_index);
-        if (selected_pages[global_page_index] != 0U) {
-          continue;
-        }
-        if (best_candidate == nullptr
-          || candidate_page.clip_index < best_candidate->clip_index
-          || (candidate_page.clip_index == best_candidate->clip_index
-            && (candidate_page.hit_count > best_candidate->hit_count
-              || (candidate_page.hit_count == best_candidate->hit_count
-                && candidate_page.distance2_to_camera
-                  < best_candidate->distance2_to_camera)))) {
-          best_candidate = &candidate_page;
+    const auto [begin_x, end_x, begin_y, end_y] = *selection;
+    for (std::uint32_t page_y = begin_y; page_y <= end_y && pages_remaining > 0U;
+      ++page_y) {
+      for (std::uint32_t page_x = begin_x; page_x <= end_x && pages_remaining > 0U;
+        ++page_x) {
+        if (mark_selected_page(clip_index, page_x, page_y)) {
+          --pages_remaining;
         }
-        break;
       }
     }
-
-    if (best_candidate == nullptr) {
-      break;
-    }
-
-    if (!mark_selected_page(best_candidate->clip_index, best_candidate->page_x,
-          best_candidate->page_y)) {
-      break;
-    }
-    --pages_remaining;
+    --clips_with_hits;
   }

   if (std::none_of(selected_pages.begin(), selected_pages.end(),
@@ -1100,9 +1207,8 @@ auto VirtualShadowMapBackend::BuildDirectionalVirtualViewState(
         const float right = logical_right + page_guard_world;
         const float guarded_bottom = bottom - page_guard_world;
         const float guarded_top = top + page_guard_world;
-        const glm::mat4 light_proj
-          = glm::orthoRH_ZO(
-            left, right, guarded_bottom, guarded_top, near_plane, far_plane);
+        const glm::mat4 light_proj = glm::orthoRH_ZO(left, right, guarded_bottom,
+          guarded_top, clip_near_plane[clip_index], clip_far_plane[clip_index]);

         engine::ViewConstants page_view_constants = view_constants;
         page_view_constants.SetViewMatrix(light_view)
