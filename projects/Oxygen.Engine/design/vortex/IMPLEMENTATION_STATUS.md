# Vortex Implementation Status

Status: `active tracking ledger`

This file is the current-state ledger for Vortex. It is intentionally separate
from [PLAN.md](./PLAN.md): the plan defines what should happen next, while this
file records what is known about implementation progress, validation evidence,
and remaining gaps.

## 1. How To Use This File

Before starting Vortex work:

1. Read the `Current Next Step` section.
2. Find the target milestone in the `Milestone Ledger`.
3. Check the `Open Gaps` section for blockers.
4. Define fresh verification before claiming closure.
5. Update this file with changed files, commands run, results, UE5.7 references
   checked, and remaining gaps.

This file does not treat implementation presence as completion. Many Vortex
systems have real code but still need fresh validation and UE5.7 parity proof.

## 2. Status Vocabulary

| Status | Meaning |
| --- | --- |
| `validated` | Implementation exists, docs/status are current, and fresh validation evidence is recorded here. |
| `landed_needs_validation` | Substantial code exists, but this ledger does not contain fresh closure proof. |
| `in_progress` | Implementation exists but known gaps remain, or scope is actively being corrected. |
| `planned` | Scope is defined; implementation has not started for this milestone. |
| `blocked` | Required design, dependency, or proof surface is missing. |
| `future` | Explicitly deferred beyond the production-complete desktop deferred baseline. |

## 3. Current Next Step

**Next milestone:** `VTX-M05A — Diagnostics Product Service`

**Why:** The Phase 4D environment/fog parity packages now have implementation,
docs/status, focused proof, and one consolidated VortexBasic runtime proof.
`Examples/Async` now has Vortex-backed structural/product/presentation proof.
The single-view composition and presentation contract now has focused
composition queue tests and RenderDoc-backed proof through Stage 22,
composition copy, overlay blend, and final present. The next gate is making
diagnostics a product-owned service instead of relying on proof tooling and
ad hoc debug vocabulary.

**Validated implementation work packages:** `VTX-M04D.1 — Environment
Publication And Sky/Fog Contract Truth`, `VTX-M04D.2 — UE5.7 Exponential
Height Fog Parity`, `VTX-M04D.3 — UE5.7 Local Fog Volume Parity`, and
`VTX-M04D.4 — UE5.7 Volumetric Fog Parity`, `VTX-M04D.5 — Environment Runtime
Proof And Async Preparation`, `VTX-M04D.6 — UE5.7 Aerial Perspective
Parity`, `VTX-M04E — Async Migration Parity Gate`, and `VTX-M04F —
Single-View Composition And Presentation Closeout`

**Active implementation work packages:** `VTX-M05A — Diagnostics Product
Service`

**Validated VTX-M04D.1 workstream plan:**
[plan/VTX-M04D.1-environment-publication-truth.md](./plan/VTX-M04D.1-environment-publication-truth.md)

- stable sky/atmosphere and below-horizon behavior was preserved
- SkyLight/IBL publications now distinguish authored, valid, unavailable, and
  stale states
- Stage 14 environment execution state is visible at the SceneRenderer boundary
- environment binding/static/view/product publication tests were extended
- validation evidence is recorded below before the validated status claim

**Validated VTX-M04D.2 workstream plan:**
[plan/VTX-M04D.2-exponential-height-fog-parity.md](./plan/VTX-M04D.2-exponential-height-fog-parity.md)

- VTX-M04D.2 has implementation, UE5.7 grounding, focused tests, prior shader
  validation, focused VortexBasic runtime/capture proof, city-scale RenderScene
  capture proof, and doc/status consistency evidence recorded in this ledger.
- Height-fog cubemap inscattering remains explicitly unavailable and deferred.
  It is not the current priority for city-scale environment/fog artifact
  closure.
- Immediate priority has moved to VTX-M05A diagnostics product service:
  DemoShell environment override behavior, local-fog view enablement,
  aerial-perspective volume resolution/depth defaults, height fog, local fog,
  volumetric fog, city-scale CSM hydration, and main-view AP compose behavior
  now have implementation and focused proof evidence. The consolidated
  VTX-M04D.5 proof path covers the validated AP/fog packages plus authored
  SkyLight unavailable/volumetric state. VTX-M04E now covers the canonical
  Async runtime path, and VTX-M04F now covers the single-view
  composition/presentation path.

## 4. Milestone Ledger

| ID | Milestone | Status | Implementation Evidence | Missing To Close |
| --- | --- | --- | --- | --- |
| VTX-M00 | Planning and status truth surface | `validated` | `PLAN.md` has been rewritten as a milestone-first plan; this ledger exists; restricted doc scans and `git diff --check` passed on 2026-04-25. | No open planning-status gap for this docs-only milestone. |
| VTX-M01 | Renderer Core and SceneRenderer baseline | `landed_needs_validation` | Vortex module, Renderer Core, publication, upload/resource substrate, SceneRenderer shell, SceneTextures, non-runtime facades, resolve/cleanup, and related tests are present in source. | Fresh build/test evidence and any required parity proof are not recorded in this ledger. |
| VTX-M02 | Deferred core visual path | `landed_needs_validation` | InitViews, depth prepass, generic Screen HZB, base pass/GBuffer/velocity, Stage 10 publication, deferred lighting, shader families, debug views, and focused tests/tools are present. | Fresh build, ShaderBake/catalog validation, tests, and capture/analyzer proof are not recorded here. |
| VTX-M03 | Migration-critical non-environment services | `landed_needs_validation` | LightingService, ShadowService directional baseline, PostProcessService, Stage 8/12/22 routing, and focused tests are present. | Service-specific fresh validation, RenderDoc proof, and residual parity review are not recorded here. |
| VTX-M04D | Environment / fog parity closure | `validated` | Environment publication truth, main-view aerial perspective, height fog, local fog, volumetric fog, and consolidated runtime proof are validated in VTX-M04D.1 through VTX-M04D.6. | Real SkyLight cubemap capture/filtering remains a separate IBL/indirect-lighting resource implementation before usable IBL output can be claimed. Reflection/360 AP remains deferred to the future reflection-capture resource path. |
| VTX-M04D.1 | Environment publication and sky/fog contract truth | `validated` | EnvironmentLightingService now sanitizes IBL probe bindings, distinguishes authored SkyLight from usable IBL resources, keeps invalid SkyLight/volumetric products explicit, exposes Stage 14 local-fog state through SceneRenderer, and focused tests passed on 2026-04-25. | Real SkyLight capture/filtering and aerial-perspective parity remain outside VTX-M04D.1. Height fog, local fog, and volumetric fog are validated separately by VTX-M04D.2 through VTX-M04D.4; VTX-M04D.1 does not claim those parity gates. |
| VTX-M04D.2 | UE5.7 exponential height fog parity | `validated` | Height fog model/pass code exists; VTX-M04D.2 widens the CPU/HLSL fog payload, translates primary/secondary layers, start/end/cutoff/max-opacity, directional and sky-atmosphere controls, removes the simplified fog fallback path, replaces the fog pass shader with a `HeightFogCommon`-shaped line-integral path, preserves far-depth sky exclusion, records focused build/test/ShaderBake evidence below, and now has focused VortexBasic enabled/disabled RenderDoc proof plus city-scale RenderScene capture proof. | Cubemap inscattering resource binding/sampling remains explicitly unavailable and deferred; it is not a blocker for the current city-scene AP/fog priority. Real SkyLight capture/filtering and full environment runtime closure remain later milestones. |
| VTX-M04D.3 | UE5.7 local fog volume parity | `validated` | Analytical local-fog volume path is implemented and proven. Evidence covers UE5.7 source mapping, authoring sanitization, sorting/capping, HZB-backed tiled culling, UE-shaped single draw-indirect rendering, analytical shader path, SceneColor contribution, far-depth no-op behavior, focused tests, ShaderBake/catalog validation, VortexBasic runtime/capture proof, and focused RenderDoc draw-args probe. | Local-fog participating-media injection into volumetric fog is validated separately under VTX-M04D.4 and is not part of the VTX-M04D.3 claim. |
| VTX-M04D.4 | UE5.7 volumetric fog parity | `validated` | Volumetric fog model/publication seams exist; a Vortex-native Stage-14 compute path allocates an `IntegratedLightScattering` 3D product for authored/requested volumetric fog, publishes SRV/static bindings, and composes it in Stage 15 height fog. Focused build/test/shader/runtime-smoke evidence and focused VortexBasic RenderDoc analyzer evidence are recorded below, including captured Stage-15 fog `EnvironmentStaticData` SRV/flag/grid proof and captured integrated-volume min/max/slice proof. Directional conventional CSM projected shadows are validated as a prerequisite blocker: Stage 8 writes all cascades, Stage 12 consumes `Vortex.DirectionalShadowSurface`, receiver samples include lit and shadowed points, `directional-shadow-mask` renders a 0..1 mask, the D3D12 debug-layer audit passes, and the user visually confirmed projected shadows. The city-scale follow-up fixed cooked directional-light CSM hydration so scene-authored split mode, max shadow distance, transition, and fadeout settings reach runtime lights. The volumetric compute path uses UE5.7-shaped logarithmic froxel depth, evaluates spatial primary/secondary height-fog media density, samples the validated primary directional CSM product for shadowed directional light injection, injects local-fog participating media from the validated Stage-14 tiled local-fog products, injects Oxygen's distant-SkyLight LUT as a volumetric ambient SkyLight term, and uses UE-shaped temporal jitter/reset/history-miss reprojection on the Oxygen single integrated-scattering product. Local-fog, SkyLight, directional-shadow, and temporal term-isolated artifact proof exists from paired focused VortexBasic captures. RenderScene city-scale proof now loads `CityEnvironmentValidation` deterministically, captures frame 90, validates the city integrated-scattering volume and Stage-15 fog payload, and passes a CDB/D3D12 debug-layer audit. | No remaining VTX-M04D.4 closure gap. Accepted Oxygen divergences are documented for the single integrated-scattering temporal product, without UE conservative-depth history fixup or pre-exposure transfer. Real SkyLight cubemap capture/filtering remains an IBL/indirect-lighting resource gap before usable IBL output can be claimed, not a VTX-M04D.4 closure gate. |
| VTX-M04D.5 | Environment runtime proof and Async preparation | `validated` | `Run-VortexBasicRuntimeValidation.ps1` now builds VortexBasic, runs a CDB/D3D12 debug-layer audit, captures RenderDoc frame 5, and asserts one runtime path with atmosphere, main-view AP, height fog, local fog, volumetric fog, authored SkyLight unavailable state, and SkyLight volumetric injection. Evidence is recorded below. | Real SkyLight cubemap capture/filtering remains a separate IBL/indirect-lighting resource implementation before usable IBL output can be claimed. Async migration proof moves to VTX-M04E. |
| VTX-M04D.6 | UE5.7 aerial perspective parity | `validated` for main-view AP | Vortex AP sampling uses UE5.7-shaped camera-volume lookup helpers, removes stale approximation wording, keeps camera-volume generation raw while AP strength remains an apply-time view control, exposes effective DemoShell/VortexBasic controls, fixes city-scale reverse-Z far-depth AP composition, and has focused enabled/disabled plus city-scale RenderDoc proof recorded below. | Reflection/360-view AP is explicitly deferred to the future reflection-capture resource path and is not claimed. Consolidated environment runtime proof is validated separately by VTX-M04D.5. |
| VTX-M04E | Async migration parity gate | `validated` | Async now authors a Vortex-ready scene environment, sun directional light, shadowed receiver ground, lifted sphere and two-submesh geometry, and Vortex runtime view metadata. DemoShell no longer overrides the scene-authored environment in this example. `AnalyzeRenderDocAsyncProducts.py` now proves camera aerial-perspective consumption from bound Stage-15 pixel resources before falling back to RenderDoc usage events. Full runtime proof passed on 2026-04-26; evidence is recorded below. | No open M04E closure gap. |
| VTX-M04F | Single-view composition and presentation closeout | `validated` | Runtime composition registration, queued single-view composition copy, Stage 22 post-process routing, and overlay blend path exist. `AnalyzeRenderDocAsyncProducts.py` now gates the proof on exactly one composition copy from `Async.SceneColor` after Stage 22, exactly one overlay blend after that scene copy, final present output, and focused `RendererCompositionQueue` tests. Runtime proof passed on 2026-04-26; evidence is recorded below. | No open M04F closure gap. |
| VTX-M05A | Diagnostics product service | `planned` | Diagnostics substrate pieces and design exist. | Concrete DiagnosticsService product surface, debug modes, overlays/panels/timeline/debug bindings. |
| VTX-M05B | Occlusion consumer closeout | `planned` | Generic Screen HZB implementation and bindings exist. | Full occlusion query/consumer policy, visibility classifications, batching, temporal handoff. |
| VTX-M05C | Translucency stage | `planned` | Directory placeholders and LLD exist. | Stage 18 implementation consuming lighting/shadow/environment publications. |
| VTX-M05D | Local-light conventional shadow expansion | `planned` | Directional ShadowService baseline exists; local-light LLD exists. | Spot-light shadows, point-light strategy, publication/capture proof. |
| VTX-M06A | Multi-view proof closeout | `planned` | CompositionView/ViewLifecycle/CompositionPlanner substrate exists. | Heterogeneous per-view runtime proof, PiP/multi-surface routing, per-view product isolation. |
| VTX-M06B | Offscreen proof closeout | `planned` | `ForOffscreenScene` API and presets exist. | Focused tests and runtime/capture validation for offscreen deferred/forward scenarios. |
| VTX-M06C | Feature-gated runtime variants | `planned` | Capability vocabulary and many services exist. | Depth-only, shadow-only, no-environment, no-shadowing, no-volumetrics, diagnostics-only validation. |
| VTX-M07 | Production readiness and legacy retirement | `planned` | Not applicable. | All required examples/tests ported, legacy renderer removal plan and proof. |
| VTX-FUTURE | Reserved post-baseline families | `future` | Some placeholder directories or shader inventory may exist. | Geometry virtualization, material composition, indirect lighting/GI/reflections, VSM, clouds, heterogeneous volumes, water, hair, distortion, light shafts. |

## 5. Achievement Ledger

These entries are achievements visible from source/doc inspection. They are not
fresh closure claims unless the status is `validated`.

| Area | Known Achievement | Status | Notes |
| --- | --- | --- | --- |
| Build/module scaffold | Vortex module and CMake target exist. | `landed_needs_validation` | Older plan text that said the scaffold was not wired was stale and has been removed. |
| Renderer Core substrate | Renderer, render context materialization, upload/resources, publication, composition queue, and facade patterns exist. | `landed_needs_validation` | Needs fresh build/test record before validated status. |
| SceneTextures | Concrete scene texture product, setup mode, bindings, extracts, and tests exist. | `landed_needs_validation` | Future GBufferE/F remain reserved. |
| SceneRenderer shell | SceneRenderer construction, stage ordering, dispatch skeleton, resolve, cleanup, and tests exist. | `landed_needs_validation` | Stage 14 wording must remain aligned with active environment ownership. |
| Deferred core | InitViews, depth prepass, base pass, velocity, GBuffer publication, deferred lighting, and debug visualization have implementation surfaces. | `landed_needs_validation` | Needs fresh ShaderBake/test/capture evidence before parity closure. |
| LightingService | Service exists and Stage 12 routes through it. | `landed_needs_validation` | Validate Stage 6/12 publication and deferred-light ownership. |
| ShadowService | Directional conventional shadow service exists. | `validated` | Directional CSM now propagates authored split/fade/bias settings into Stage 8/12 metadata, follows Oxygen reversed-Z depth conventions, binds shadow-depth pass constants directly, keeps CPU/HLSL shadow-frame packing in lockstep, and has RenderDoc receiver-pixel plus user visual proof for projected shadows. The scene-loader follow-up preserves cooked manual CSM split mode, max shadow distance, transition, and fadeout settings for city-scale directional lights, and the user visually confirmed `CityEnvironmentValidation` now has shadows. Local-light shadows remain future VTX-M05D work. | Validated for conventional directional CSM projected shadows only. Spot/point/local-light shadows, VSM, and full volumetric-fog shadowed-light parity remain separate work. |
| PostProcessService | Exposure/bloom/tonemap service exists and Stage 22 is routed. | `landed_needs_validation` | Exposure parity improvement remains a separate tracked risk in the post-process LLD set. |
| Environment sky/atmosphere | Atmosphere state, LUTs, sky-view, aerial perspective, sky compose, dual light state, and stable below-horizon design invariants exist. | `validated` for VTX-M04D.6 main-view AP | Preserve this work; do not reopen below-horizon remediation as a separate doc. VTX-M04D.6 has focused AP implementation evidence, focused enabled/disabled RenderDoc proof, and city-scale RenderScene capture proof. Reflection/360-view AP is explicitly deferred. |
| Environment fog | Height fog, local fog, and volumetric fog surfaces exist. | `validated` for VTX-M04D.2/VTX-M04D.3/VTX-M04D.4 fog packages | Height fog, local fog, and volumetric fog package gates are validated. Consolidated runtime proof is validated by VTX-M04D.5. |
| Generic Screen HZB | Stage 5 Screen HZB module, shader, publications, and tests exist. | `landed_needs_validation` | Environment local fog depends on generic HZB publication, not full occlusion closure. |
| Non-runtime facades | Single-pass and render-graph harnesses have APIs and tests; offscreen API exists. | `landed_needs_validation` | Offscreen proof is still a separate milestone. |
| Runtime proof tooling | Vortex runtime/capture/analyzer tooling exists. | `landed_needs_validation` | Tool availability is not proof; wrappers/analyzers/assertions must pass per milestone. |

## 6. Open Gaps

| Gap | Blocks | Required Resolution |
| --- | --- | --- |
| No fresh validation recorded in this new status ledger for existing landed systems. | Any `validated` status for VTX-M01 through VTX-M03. | Run focused builds/tests and record exact command output/results. |
| UE-specific temporal refinements beyond Oxygen's single integrated-scattering product are not implemented. | Future stricter UE temporal parity work, if required beyond VTX-M04D.4. | VTX-M04D.4 records this as an accepted Oxygen divergence: conservative-depth history fixup, pre-exposure transfer, and UE's separate light-scattering history product are not claimed by the validated Oxygen path. |
| Real SkyLight capture/filtering is not implemented. | Future SkyLight lighting parity and downstream image-based lighting quality. | VTX-M04D.1 now publishes enabled SkyLight as unavailable until usable IBL resources exist. VTX-M04D.4 may use the proven Oxygen distant-sky LUT volumetric ambient path; real cubemap capture/filtering remains a later environment/indirect-lighting work package before claiming usable IBL output. |
| Reflection/360-view aerial perspective lacks a runtime resource path. | Future reflection-capture parity. | Explicitly deferred from VTX-M04D.6; implement with the future reflection-capture resource path before claiming reflection/360 AP parity. |
| Diagnostics product surface is not closed. | M05A and later observability. | Land concrete service surface without confusing proof tooling with diagnostics. |
| Translucency stage is not implemented. | M05C and later multi-view/offscreen feature matrices. | Implement Stage 18 forward-lit translucent rendering. |
| Full occlusion consumer policy is not closed. | M05B and advanced visibility claims. | Build occlusion/query/consumer policy over generic HZB. |

## 7. Environment Detailed Tracker

| Environment Feature | Current State | Target Milestone | Closure Evidence Required |
| --- | --- | --- | --- |
| Below-horizon sky behavior | Stable design invariants captured in `environment-service.md`; separate remediation doc removed. | VTX-M04D.1 preserve | Regression tests/capture proof that raw light directions, LUT domains, horizon seam behavior, and sky-depth rules remain stable. |
| Atmosphere LUT family | Implementation exists for transmittance, multi-scattering, distant sky light, sky view, and camera aerial perspective. VTX-M04D.6 now has focused source/test/shader-bake evidence, focused VortexBasic enabled/disabled proof, and city-scale RenderScene AP proof for the main-view camera AP path. | VTX-M04D.1 preserve / VTX-M04D.6 main-view AP parity | `validated` for main-view AP; reflection/360-view behavior remains explicitly deferred. |
| SkyLight / IBL | Publication truth is validated: authored SkyLight no longer enables shader-visible IBL without usable resources, probe revisions do not imply valid resource slots, and stale source changes invalidate prior probe bindings. | VTX-M04D.1 validated | Real capture/filtering resource generation remains a later implementation gap; current contract publishes explicit unavailable state. |
| Height fog | UE5.7-informed analytic height-fog payload, translation, shader integral, sky-depth exclusion, focused tests, focused VortexBasic enabled/disabled runtime/capture proof, and city-scale RenderScene capture proof exist; cubemap resource sampling remains unavailable. | VTX-M04D.2 validated | `validated`; cubemap inscattering resource binding/sampling is deferred as a nice-to-have and must not displace AP/fog artifact fixes. |
| Local fog volumes | Analytical local-fog volume parity is validated for Stage 14 tiled culling and Stage 15 compose. Evidence records UE5.7 references, authoring sanitization, UE-shaped max-instance cap range, UE-shaped single draw-indirect tiled rendering, SceneColor contribution, and far-depth no-op proof. | VTX-M04D.3 validated | Local-fog participating-media injection into volumetric fog is validated separately under VTX-M04D.4. |
| Volumetric fog | Validated VTX-M04D.4 path: authored/requested volumetric fog creates and publishes an `IntegratedLightScattering` 3D product consumed by Stage 15 fog; focused CPU/shader/publication tests exist; focused VortexBasic RenderDoc proof validates the Stage-14 volumetric dispatch, named integrated-scattering product write, captured Stage-15 fog static SRV/flag/grid payload, and sampled nonzero integrated-scattering volume slices. Directional conventional CSM projected-shadow proof is closed as a prerequisite, and cooked CSM hydration preserves city-scale directional-light ranges. UE5.7-shaped logarithmic froxel depth, spatial height-fog media density, primary directional CSM shadowed-light sampling, local-fog participating-media injection, Oxygen distant-SkyLight ambient injection, and UE-shaped temporal jitter/reset/history-miss reprojection have focused CPU/shader/runtime-smoke evidence. Paired VortexBasic RenderDoc captures prove local-fog, SkyLight, directional-shadow, and temporal term deltas in the integrated-scattering product; city-scale RenderScene capture proof validates `CityEnvironmentValidation` with 3 local-fog volumes, volumetric shadows, SkyLight, temporal reprojection, Stage-15 fog payload binding, and a passing D3D12 debug-layer audit. | VTX-M04D.4 | `validated`; no remaining VTX-M04D.4 closure gap. Real SkyLight capture/filtering is an IBL/indirect-lighting resource gap outside VTX-M04D.4. |
| Environment runtime proof | `Run-VortexBasicRuntimeValidation.ps1` now proves one VortexBasic runtime path with atmosphere, main-view AP, height fog, local fog, volumetric fog, authored SkyLight unavailable state, and SkyLight volumetric injection. | VTX-M04D.5 | `validated`; Async proof is now validated separately by VTX-M04E. |
| Async runtime proof | `Run-AsyncRuntimeValidation.ps1` proves the canonical Async example on Vortex with Stage 3/8/12/15/22 structural ordering, environment product publication, camera aerial-perspective volume consumption, final present output, overlay composition, and the single-view composition/presentation contract. | VTX-M04E / VTX-M04F | `validated`; next work is VTX-M05A diagnostics product service. |
| Single-view composition/presentation proof | `AnalyzeRenderDocAsyncProducts.py` now validates Stage 22 tonemap output, one post-Stage-22 composition copy from `Async.SceneColor`, one overlay blend after the scene copy, and nonzero final present output. Focused `RendererCompositionQueue` tests prove queued single-view composition behavior. | VTX-M04F | `validated`; no remaining M04F closure gap. |

## 8. Validation Evidence Log

Append new entries at the top.

| Date | Scope | Commands / Evidence | Result | Residual Gap |
| --- | --- | --- | --- | --- |
| 2026-04-26 | VTX-M04F single-view composition and presentation closeout | Changed tools/docs: `tools/vortex/AnalyzeRenderDocAsyncProducts.py`, `design/vortex/PLAN.md`, and this ledger. Existing runtime composition code is proven as the M04F implementation surface: Stage 22 writes `Async.SceneColor`; runtime composition queues one single-view copy into the presentation target; the overlay blend occurs after that scene copy; final present remains nonzero. The Async product analyzer now requires `compositing_copy_scope_count_match=true`, `compositing_copy_event_count_match=true`, `compositing_copy_after_stage22=true`, `compositing_copy_uses_stage22_output=true`, `compositing_copy_source_resource_name=Async.SceneColor`, `imgui_overlay_blend_scope_count_match=true`, `imgui_overlay_blend_draw_count_match=true`, and `imgui_overlay_after_scene_copy=true` in addition to the existing final-present/product gates. Tool syntax passed: `python -m py_compile tools\vortex\AnalyzeRenderDocAsyncProducts.py`. Focused build passed: `cmake --build out\build-ninja --config Debug --target oxygen-examples-async --parallel 4`. Runtime/capture proof passed: `powershell -NoProfile -ExecutionPolicy Bypass -File tools\vortex\Run-AsyncRuntimeValidation.ps1 -Output out\build-ninja\analysis\vortex\m04f-single-view-proof\current -Frame 90 -RunFrames 94 -Fps 30 -BuildJobs 4`. Product report `out\build-ninja\analysis\vortex\m04f-single-view-proof\current\current_renderdoc.rdc_async_products_report.txt` records `overall_verdict=pass`, `stage22_tonemap_output_nonzero=true`, `stage22_sampled_tonemap_output_nonzero=true`, `final_present_nonzero=true`, `compositing_copy_after_stage22=true`, `compositing_copy_uses_stage22_output=true`, `compositing_copy_source_resource_name=Async.SceneColor`, `imgui_overlay_after_scene_copy=true`, and `final_present_vs_tonemap_changed=true`. Focused composition queue tests passed: `ctest --preset test-debug -R "RendererCompositionQueue" --output-on-failure` with 5/5 tests passing. | `validated`; VTX-M04F has implementation, docs/status, tool syntax, focused build, RenderDoc product proof, final present proof, overlay ordering proof, and focused queue tests. | No M04F closure gap. DiagnosticsService, occlusion, translucency, local-light shadows, multi-view, and offscreen rendering remain future milestones. |
| 2026-04-26 | VTX-M04E Async migration parity gate | Changed code/tools/docs: `Examples/Async/MainModule.cpp`, `Examples/Async/MainModule.h`, `tools/vortex/AnalyzeRenderDocAsyncProducts.py`, `design/vortex/PLAN.md`, and this ledger. Async now authors a scene-owned `SceneEnvironment` with SkyAtmosphere, SkyLight, and height fog; creates a shadow-casting sun directional light; disables DemoShell forced environment override for this scene; requests atmosphere and height fog in the Vortex runtime view metadata; adds a large shadow/atmosphere receiver ground plane; raises the sphere orbit and two-submesh triangle geometry above the ground; and keeps scene geometry shadow participation explicit. The Async product analyzer now proves `Vortex.Environment.AtmosphereCameraAerialPerspective` consumption from Stage-15 pixel shader bound read-only resources before falling back to RenderDoc event usage. Focused build passed: `cmake --build out\build-ninja --config Debug --target oxygen-examples-async --parallel 4`. Full runtime/capture proof passed: `powershell -NoProfile -ExecutionPolicy Bypass -File tools\vortex\Run-AsyncRuntimeValidation.ps1 -Output out\build-ninja\analysis\vortex\m04e-async-proof\current -Frame 90 -RunFrames 94 -Fps 30 -BuildJobs 4`. Validation report `out\build-ninja\analysis\vortex\m04e-async-proof\current\current_renderdoc.rdc.validation.txt` records `analysis_result=success`, `overall_verdict=pass`, `visual_psnr_pass=true`, `depth_max_error_pass=true`, `async_runtime_stage_order_valid=true`, `stage8_scope_present=true`, `stage12_directional_scope_present=true`, `stage12_spot_scope_present=true`, `stage15_sky_scope_present=true`, `stage15_atmosphere_scope_present=true`, `stage15_fog_scope_present=true`, `stage22_tonemap_scope_present=true`, `stage8_shadow_depth_nonzero=true`, `stage12_directional_scene_color_nonzero=true`, `stage12_spot_scene_color_nonzero=true`, `stage15_sky_scene_color_changed=true`, `stage15_atmosphere_scene_color_changed=true`, `stage15_fog_scene_color_changed=true`, `stage22_tonemap_output_nonzero=true`, and `stage15_far_background_mask_valid=true`. Product report `current_renderdoc.rdc_async_products_report.txt` records `overall_verdict=pass`, `atmosphere_camera_aerial_consumed=true`, `atmosphere_camera_aerial_resource_name=Vortex.Environment.AtmosphereCameraAerialPerspective`, `final_present_nonzero=true`, and `imgui_overlay_composited_on_scene=true`. | `validated`; VTX-M04E has implementation, docs/status, focused build evidence, RenderDoc structural/product proof, final present proof, and overlay composition proof. | Real SkyLight cubemap capture/filtering remains a later IBL/indirect-lighting resource gap; reflection/360 AP remains deferred. VTX-M04F now validates single-view composition/presentation separately. |
| 2026-04-26 | VTX-M04D.5 consolidated environment runtime proof | Changed code/tools/docs: `src/Oxygen/Vortex/Environment/EnvironmentLightingService.cpp`, `tools/vortex/Run-VortexBasicRuntimeValidation.ps1`, `tools/vortex/Assert-VortexBasicRuntimeProof.ps1`, `design/vortex/PLAN.md`, and this ledger. The environment service now logs per-view product publication truth for runtime proof: environment view/products, atmosphere LUT cache, transmittance, multi-scattering, distant SkyLight LUT, sky-view LUT, camera aerial perspective, authored SkyLight unavailable/valid state, authored volumetric fog, and integrated light scattering. The durable VortexBasic proof path now runs both debugger-audit and RenderDoc capture with `--aerial-start-depth 0 --aerial-scattering-strength 1`, so Stage-15 atmosphere AP has measurable output. The runtime assertion now gates concrete M04D.5 evidence: CLI flags for atmosphere/height/local/volumetric fog and AP controls, product publication truth, authored SkyLight unavailable state, volumetric height-fog media execution, SkyLight volumetric injection, local-fog volumetric injection, temporal reprojection execution, Stage-14 local/volumetric dispatches, Stage-15 sky/atmosphere/fog draws, Stage-15 sky/atmosphere/fog `SceneColor` changes, local fog composed in volumetric, final present nonzero, and clean D3D12 audit. PowerShell parser checks passed for `tools\vortex\Assert-VortexBasicRuntimeProof.ps1` and `tools\vortex\Run-VortexBasicRuntimeValidation.ps1`. Consolidated proof passed: `powershell -NoProfile -ExecutionPolicy Bypass -File tools\vortex\Run-VortexBasicRuntimeValidation.ps1 -Output out\build-ninja\analysis\vortex\m04d5-runtime-proof\vortexbasic-full-tight -Frame 5 -RunFrames 9 -Fps 30 -BuildJobs 4`. The wrapper build step exited `0` for `cmake --build out\build-ninja --config Debug --target oxygen-vortex oxygen-graphics-direct3d12 oxygen-examples-vortexbasic --parallel 4`. Validation report `out\build-ninja\analysis\vortex\m04d5-runtime-proof\vortexbasic-full-tight.validation.txt` records `analysis_result=success`, `overall_verdict=pass`, `runtime_exit_code=0`, `d3d12_error_count=0`, `dxgi_error_count=0`, `blocking_warning_count=0`, `transmittance_lut_published=true`, `multi_scattering_lut_published=true`, `distant_sky_light_lut_published=true`, `sky_view_lut_published=true`, `camera_aerial_perspective_published=true`, `atmosphere_lut_cache_valid=true`, `runtime_environment_products_published=true`, `runtime_sky_light_authored_enabled=true`, `runtime_sky_light_ibl_unavailable=true`, `runtime_volumetric_fog_height_fog_media_executed=true`, `runtime_volumetric_fog_sky_light_injection_executed=true`, `runtime_volumetric_fog_local_fog_injection_executed=true`, `runtime_volumetric_fog_temporal_reprojection_executed=true`, `stage14_local_fog_scope_count_match=true`, `stage14_volumetric_fog_scope_count_match=true`, `stage15_sky_scene_color_changed=true`, `stage15_atmosphere_scene_color_changed=true`, `stage15_fog_scene_color_changed=true`, and `final_present_nonzero=true`. Debug audit report records one accepted shutdown live-factory warning and no blocking debug-layer findings. Focused unit tests passed: `ctest --preset test-debug -R "Oxygen.Vortex.EnvironmentLightingService" --output-on-failure` with `40/40` tests. | `validated`; VTX-M04D.5 now has implementation, docs/status, parser checks, focused tests, debugger-backed D3D12 audit, RenderDoc capture/analyzer/assertion evidence, and one repeatable runtime proof path across the validated AP/fog packages plus authored SkyLight state. | Async runtime proof is now validated by the newer VTX-M04E row. Real SkyLight cubemap capture/filtering remains a separate IBL/indirect-lighting resource gap before usable IBL output can be claimed. Reflection/360-view AP remains deferred to the future reflection-capture resource path. |
| 2026-04-26 | VTX-M04D.6 main-view aerial perspective capture closure and city fog tuning | Changed code/tools/content/docs: `Examples/DemoShell/Runtime/DemoAppContext.h`, `Examples/VortexBasic/main_impl.cpp`, `Examples/VortexBasic/MainModule.cpp`, `src/Oxygen/Graphics/Direct3D12/Shaders/Vortex/Services/Environment/AtmosphereCompose.hlsl`, `Examples/Content/scenes/city-environment-validation/CityEnvironmentValidation.scene.json`, `tools/vortex/AnalyzeRenderDocVortexAerialPerspective.py`, `tools/vortex/Assert-VortexAerialPerspectiveProof.ps1`, `tools/vortex/Verify-VortexAerialPerspectiveProof.ps1`, `tools/vortex/Verify-VortexHeightFogProof.ps1`, `tools/vortex/README.md`, `design/vortex/PLAN.md`, `design/vortex/plan/VTX-M04D.6-aerial-perspective-parity.md`, and this ledger. UE5.7 grounding checked: `SkyAtmosphereCommon.ush`, `SkyAtmosphere.usf`, `BasePassPixelShader.usf`, `BasePassVertexShader.usf`, `SceneRendering.cpp`, and `SkyAtmosphereRendering.cpp` for AP lookup, camera-volume generation, base-pass non-sky application, view publication, LUT dimensions/depth, and dummy camera-volume resources. Implementation adds VortexBasic AP strength/start-depth proof controls, makes VortexBasic atmosphere metadata respect `--with-atmosphere`, adds a focused RenderDoc analyzer/assert wrapper, fixes AP compose far-background gating from any nonzero far mask to `far_background > 0.999` for Oxygen reversed-Z city geometry, and restores `CityEnvironmentValidation` `aerial_perspective_distance_scale` from the old compensation value `700` to `1.0`. Parser checks passed: `python -m py_compile tools\vortex\AnalyzeRenderDocVortexAerialPerspective.py`; PowerShell parser checks passed for the AP assert/verify scripts and the corrected height-fog verifier. Builds/tests passed: `cmake --build out\build-ninja --config Debug --target oxygen-examples-vortexbasic --parallel 4`; `cmake --build out\build-ninja --config Debug --target Oxygen.Examples.RenderScene.exe --parallel 4`; `cmake --build --preset windows-debug --target oxygen-graphics-direct3d12 --parallel 4`; `ShaderBake rebuild ... --mode dev` wrote `184` modules; shader catalog tests passed `4/4`; `cmake --build --preset windows-debug --target Oxygen.Vortex.EnvironmentLightingService Oxygen.Vortex.SceneRendererPublication oxygen-examples-vortexbasic Oxygen.Examples.RenderScene.exe --parallel 4`; `ctest --preset test-debug -R "Oxygen.Vortex.(EnvironmentLightingService\|SceneRendererPublication)" --output-on-failure` passed EnvironmentLightingService `40/40` and SceneRendererPublication `16/16`; recooking `CityEnvironmentValidation` passed with `13` jobs succeeded. Focused VortexBasic enabled proof passed against `out\build-ninja\analysis\vortex\m04d6-aerial-proof\vortexbasic_aerial_enabled_depthfix_frame5_capture.rdc`, with validation `overall_verdict=pass`, `runtime_cli_observed=1`, `camera_aerial_volume_dims=64x64x32`, `camera_aerial_probe_rgb_sum=410703.604`, `aerial_scattering_strength=1`, `aerial_perspective_start_depth_km=0`, and `stage15_atmosphere_scene_color_delta_max=10`. Focused disabled proof passed against `vortexbasic_aerial_disabled_depthfix_frame5_capture.rdc`, with `overall_verdict=pass`, `runtime_cli_observed=0`, `aerial_scattering_strength=0`, and `stage15_atmosphere_scene_color_delta_max=0`. Tuned city RenderScene proof passed against `out\build-ninja\analysis\vortex\m04d6-aerial-proof\renderscene_city_aerial_tuned_frame90_capture.rdc`, with `overall_verdict=pass`, `camera_aerial_volume_dims=64x64x32`, `camera_aerial_probe_rgb_sum=12.785145`, `aerial_perspective_distance_scale=1`, `aerial_scattering_strength=1`, `aerial_perspective_start_depth_km=0.0400000028`, `camera_aerial_consumed_by_atmosphere=true`, `static_atmosphere_camera_volume_srv_valid=true`, and `stage15_atmosphere_scene_color_delta_max=448`. | `validated` for the main-view camera aerial-perspective path; implementation, docs/status, UE5.7 source mapping, focused enabled/disabled proof, and tuned city-scale capture proof are recorded. | Reflection/360-view AP remains explicitly deferred to the future reflection-capture resource path and is not claimed. Full environment runtime closure remains VTX-M04D.5. Real SkyLight cubemap capture/filtering remains a separate IBL/indirect-lighting resource gap before usable IBL output can be claimed. |
| 2026-04-26 | VTX-M04D.2 exponential height-fog runtime/capture closure | Added proof tooling: `tools/vortex/AnalyzeRenderDocVortexHeightFog.py`, `tools/vortex/Assert-VortexHeightFogProof.ps1`, and `tools/vortex/Verify-VortexHeightFogProof.ps1`. Updated docs: `tools/vortex/README.md`, `design/vortex/PLAN.md`, `design/vortex/IMPLEMENTATION_STATUS.md`, and `design/vortex/plan/VTX-M04D.2-exponential-height-fog-parity.md`. Tool syntax checks passed: `python -m py_compile tools\vortex\AnalyzeRenderDocVortexHeightFog.py`; PowerShell parser checks passed for the two new scripts. Focused build passed: `cmake --build --preset windows-debug --target Oxygen.Vortex.EnvironmentLightingService Oxygen.Vortex.SceneRendererPublication --parallel 4`. Focused tests passed: `ctest --preset test-debug -R "Oxygen.Vortex.(EnvironmentLightingService\|SceneRendererPublication)" --output-on-failure`, with `Oxygen.Vortex.EnvironmentLightingService.Tests` 40/40 and `Oxygen.Vortex.SceneRendererPublication.Tests` 16/16. VortexBasic build passed: `cmake --build out\build-ninja --config Debug --target oxygen-examples-vortexbasic --parallel 4`. Focused enabled proof passed: `tools\vortex\Verify-VortexHeightFogProof.ps1` against `out\build-ninja\analysis\vortex\m04d2-heightfog-proof\vortexbasic_heightfog_enabled_frame5_capture.rdc`; validation records `overall_verdict=pass`, `runtime_cli_observed=true`, `stage15_fog_scope_count=1`, `stage15_fog_draw_count=1`, `height_fog_scene_color_delta_max=43`, and `height_fog_far_depth_sample_count=33`. Focused disabled proof passed: the same wrapper with `-ExpectDisabled` against `vortexbasic_heightfog_disabled_frame5_capture.rdc`; validation records `overall_verdict=pass`, `runtime_cli_observed=false`, `stage15_fog_scope_count=0`, and `stage15_fog_draw_count=0`. City-scale RenderScene proof passed: the same wrapper with `-SkipRuntimeCliCheck` against `out\build-ninja\analysis\vortex\m04d4-city-volumetric-proof\renderscene_city_volumetric_frame90_capture.rdc`; validation records `overall_verdict=pass`, `stage15_fog_scope_count=1`, `stage15_fog_draw_count=1`, `height_fog_scene_color_delta_max=593.5`, `height_fog_far_depth_sample_count=53`, captured 672-byte `EnvironmentStaticData`, positive height-fog density, valid max-opacity/min-transmittance, enabled/render-in-main-pass flags, and explicit cubemap unavailable state. | `validated`; VTX-M04D.2 now has implementation, UE5.7 grounding, focused build/test/shader evidence from the implementation slice, focused enabled/disabled runtime/capture proof, city-scale capture proof, and doc/status updates. | Cubemap inscattering resource binding/sampling remains explicitly deferred and is not claimed. The newer VTX-M04D.6 row closes main-view AP capture proof and defers reflection/360 AP; VTX-M04D.5 full environment runtime proof, Async proof, DiagnosticsService, and later systems remain open. |
| 2026-04-26 | VTX-M04D.4 RenderScene city-scale volumetric proof and debug-audit fix | Changed code/docs: `Examples/DemoShell/Runtime/DemoAppContext.h`, `Examples/RenderScene/main_impl.cpp`, `Examples/RenderScene/MainModule.{h,cpp}`, `Examples/RenderScene/README.md`, `tools/vortex/Assert-VortexBasicDebugLayerAudit.ps1`, `tools/vortex/README.md`, `design/vortex/PLAN.md`, and this ledger. RenderScene now accepts `--scene <name-or-key>`, discards stale restored scene loads when a startup scene override is active, mounts the checked-in cooked index, resolves `CityEnvironmentValidation`, and seeds explicit validation CVars so persisted local settings cannot disable local fog, local-fog-to-volumetric injection, directional volumetric shadows, or temporal reprojection. The debug-layer assertion helper now accepts `-RuntimeLogPath` for validation runs where application stdout/stderr is redirected outside the CDB transcript. Build validation passed: `cmake --build out\build-ninja --config Debug --target oxygen-examples-renderscene --parallel 4`. Startup smoke passed: `out\build-ninja\bin\Debug\Oxygen.Examples.RenderScene.exe --frames 120 --fps 0 --scene CityEnvironmentValidation --directional-shadows conventional`; `out/build-ninja/analysis/vortex/m04d4-city-startup/renderscene-city-startup-smoke-localfog.log` records stale `/Content/Scenes/NewScene1.oscene` discarded, city scene loaded from `Examples/Content/.cooked/container.index.bin`, 310 transforms uploaded, `local_fog_hzb_consumed=true`, `volumetric_fog_executed=true`, `integrated_light_scattering_valid=true`, `volumetric_fog_directional_shadowed_light_requested=true`, `volumetric_fog_height_fog_media_executed=true`, `volumetric_fog_sky_light_injection_executed=true`, `volumetric_fog_temporal_history_reprojection_executed=true`, `volumetric_fog_local_fog_injection_executed=true`, and `volumetric_fog_local_fog_instance_count=3`. RenderDoc proof passed for `out/build-ninja/analysis/vortex/m04d4-city-volumetric-proof/renderscene_city_volumetric_frame90_capture.rdc`; `Verify-VortexBasicVolumetricProof.ps1` generated `renderscene_city_volumetric_frame90_capture_vortexbasic_volumetric_report.txt.validation.txt` with `analysis_result=success`, `overall_verdict=pass`, Stage-14 volumetric dispatch count `1`, Stage-15 fog draw count `1`, valid Stage-15 `EnvironmentStaticData`, `integrated_light_scattering_volume_dims=320x175x32`, sampled RGB sum `14.3972826`, nonzero RGB, valid alpha, and `distant_sky_light_lut_resource=Vortex.Environment.DistantSkyLightLut`. CDB audit passed: `Assert-VortexBasicDebugLayerAudit.ps1 -DebuggerLogPath out\build-ninja\analysis\vortex\m04d4-city-debug-audit\renderscene_city_cdb.log -RuntimeLogPath out\build-ninja\analysis\vortex\m04d4-city-debug-audit\renderscene_city_cdb.stderr.txt -ReportPath out\build-ninja\analysis\vortex\m04d4-city-debug-audit\renderscene_city_cdb_report.txt` records `runtime_exit_code=0`, `debugger_break_detected=false`, `d3d12_error_count=0`, `dxgi_error_count=0`, `blocking_warning_count=0`, and one accepted shutdown live-factory warning. | `validated`; VTX-M04D.4 now has implementation, docs/status, focused term proof, city-scale capture proof, and debug-layer evidence. | No remaining VTX-M04D.4 closure gap. Accepted Oxygen divergences remain documented: no UE conservative-depth history fixup, no pre-exposure transfer, and no UE separate light-scattering history product. Real SkyLight cubemap capture/filtering remains outside VTX-M04D.4 as an IBL/indirect-lighting resource gap. |
| 2026-04-26 | VTX-M04D.4 SkyLight capture/filtering scope correction | Source review: Oxygen `src/Oxygen/Vortex/Environment/Passes/IblProbePass.cpp` only invalidates stale probes and reports unavailable resources unless all IBL slots already exist; `EnvironmentProbeState` and `EnvironmentProbeBindings` carry explicit valid/unavailable/stale state but no capture/filtering implementation. UE5.7 grounding checked under `F:\Epic Games\UE_5.7`: `DeferredShadingRenderer.cpp` real-time SkyLight capture scheduling/validation and `VolumetricFog.usf` SkyLight volumetric scattering are separate concerns. VTX-M04D.4 already has the Oxygen-specific distant-sky LUT volumetric ambient path with paired SkyLight term proof; real cubemap capture/filtering is required before claiming usable IBL/reflection output, but it is not a volumetric-fog closure gate. Changed docs: `design/vortex/PLAN.md` and this ledger. | `in_progress`; scope corrected before further closure claims. | VTX-M04D.4 still needs accepted temporal-divergence documentation and city-scale volumetric capture proof. Real SkyLight cubemap capture/filtering remains an environment/indirect-lighting resource implementation before usable IBL output can be claimed. |
| 2026-04-26 | VTX-M04D.4 UE-shaped temporal jitter/history-miss proof | UE5.7 grounding checked under `F:\Epic Games\UE_5.7`: `Engine/Source/Runtime/Renderer/Private/VolumetricFog.cpp` for `r.VolumetricFog.TemporalReprojection`, `r.VolumetricFog.Jitter`, `GVolumetricFogHistoryWeight=0.9`, `GVolumetricFogHistoryMissSupersampleCount=4`, Halton bases 2/3/5 through `VolumetricFogTemporalRandom`, history validity/reset/extraction, and `Engine/Shaders/Private/VolumetricFog.usf` for history UV rejection, history-miss supersampling, jitter offsets, and blending history with `HistoryWeight`. Oxygen divergence: Vortex still owns a single `IntegratedLightScattering` product rather than UE's separate material setup/light scattering/final integration products, so this slice applies UE-shaped temporal policy to that single product and does not claim UE's conservative-depth history fixup or pre-exposure transfer. Changed code/tools/docs: `Examples/DemoShell/Runtime/DemoAppContext.h`, `Examples/VortexBasic/main_impl.cpp`, `src/Oxygen/Vortex/Renderer.{h,cpp}`, `src/Oxygen/Vortex/Environment/Passes/VolumetricFogPass.{h,cpp}`, `src/Oxygen/Graphics/Direct3D12/Shaders/Vortex/Services/Environment/VolumetricFog.hlsl`, `tools/vortex/Compare-VortexBasicVolumetricTermReports.ps1`, `tools/vortex/README.md`, `design/vortex/PLAN.md`, and this ledger. Implementation adds default-on temporal/jitter CVars, history-miss sample count CVar clamped to 1..16, VortexBasic `--volumetric-temporal-reprojection`, UE-style Halton sample offsets, first-frame/reset history-miss supersampling, centered single-sample behavior when temporal reprojection is disabled, and paired comparison support for the non-additive `temporal` term. Build/shader/test validation passed: `cmake --build out\build-ninja --config Debug --target oxygen-graphics-direct3d12-shaderbake oxygen-vortex oxygen-examples-vortexbasic Oxygen.Vortex.EnvironmentLightingService.Tests --parallel 4`; ShaderBake reported `expanded_requests=184`, `dirty_requests=1`, `clean_requests=183`, `stale_requests=0`, and recompiled `CS@Vortex/Services/Environment/VolumetricFog.hlsl:VortexVolumetricFogCS`; `ctest --test-dir out\build-ninja -C Debug -R "EnvironmentLightingService" --output-on-failure` passed 42/42; PowerShell parse for `Compare-VortexBasicVolumetricTermReports.ps1` passed; `git diff --check` passed with LF/CRLF warnings only. Paired VortexBasic captures under `out/build-ninja/analysis/vortex/m04d4-term-proof-temporal` passed focused validation and comparison: enabled logs record `Parsed volumetric-temporal-reprojection option = true`, first active frame `volumetric_fog_temporal_history_reset=true`, and later active frames `volumetric_fog_temporal_history_reprojection_executed=true`; disabled logs record `Parsed volumetric-temporal-reprojection option = false` and active frames `volumetric_fog_temporal_history_requested=false`. `temporal-term-compare.txt` records enabled probe RGB sum `13.3581219`, disabled `13.3762116`, signed delta `-0.0180896999999991`, absolute delta `0.0180896999999991`, and threshold `0.001`. | `in_progress`; UE-shaped temporal jitter/reset/history-miss behavior is implemented and has paired RenderDoc artifact proof for the captured integrated-scattering product. | VTX-M04D.4 is not complete. UE conservative-depth history fixup/pre-exposure transfer, real SkyLight capture/filtering, and city-scale volumetric capture proof remain open before any full volumetric-fog parity claim. |
| 2026-04-26 | VTX-M04D.4 directional-shadow term-isolated volumetric artifact proof | UE5.7 grounding remains the previously checked VTX-M04D.4 directional shadow slice: `F:\Epic Games\UE_5.7\Engine\Shaders\Private\VolumetricFog.usf` multiplies directional light scattering by the per-froxel shadow factor, while Vortex consumes the validated conventional CSM product through Oxygen's `DirectionalShadowCommon` helper. Changed code/tools/docs: `Examples/DemoShell/Runtime/DemoAppContext.h`, `Examples/VortexBasic/main_impl.cpp`, `src/Oxygen/Vortex/Renderer.{h,cpp}`, `src/Oxygen/Vortex/Environment/Passes/VolumetricFogPass.cpp`, `tools/vortex/Compare-VortexBasicVolumetricTermReports.ps1`, `tools/vortex/README.md`, `design/vortex/PLAN.md`, and this ledger. Implementation adds default-on `vtx.volumetric_fog.directional_shadows`, VortexBasic proof option `--volumetric-directional-shadows`, and paired comparator support for a positive disabled-minus-enabled decrease for the `directional-shadow` term. Syntax/build/test validation passed: PowerShell parse for `Compare-VortexBasicVolumetricTermReports.ps1`; `git diff --check` with LF/CRLF warnings only; `cmake --build out\build-ninja --config Debug --target oxygen-vortex oxygen-examples-vortexbasic Oxygen.Vortex.EnvironmentLightingService.Tests Oxygen.Vortex.SceneRendererPublication.Tests --parallel 4`; and `ctest --test-dir out\build-ninja -C Debug -R "EnvironmentLightingService|SceneRendererPublication" --output-on-failure` with 58/58 passing. Paired VortexBasic RenderDoc captures under `out/build-ninja/analysis/vortex/m04d4-term-proof-shadow` passed focused validation. Runtime logs record `Parsed volumetric-directional-shadows option = true/false` and active frames with `volumetric_fog_directional_shadowed_light_requested=true` for enabled and `false` for disabled. Comparison report `shadow-term-compare.txt` records enabled probe RGB sum `13.3762116`, disabled probe RGB sum `13.5017905`, signed delta `-0.125578900000001`, and required shadow decrease `0.125578900000001` against threshold `0.001`. | `in_progress`; directional-shadow term-isolated artifact proof is now validated against paired captured `IntegratedLightScattering` volumes. | VTX-M04D.4 is not complete. Temporal proof is recorded in the newer row above; UE conservative-depth/pre-exposure temporal refinements, real SkyLight capture/filtering, and city-scale volumetric capture proof remain open. |
| 2026-04-26 | VTX-M04D.4 local-fog and SkyLight term-isolated volumetric artifact proof | UE5.7 grounding kept to the previously checked `F:\Epic Games\UE_5.7\Engine\Shaders\Private\VolumetricFog.usf`: local-fog volume injection adds LFV extinction/scattering/emissive into froxel media, and SkyLight volumetric scattering is gated by SkyLight volumetric intensity before being added to `LightScattering`. Changed code/tools/docs: `Examples/DemoShell/Runtime/DemoAppContext.h`, `Examples/VortexBasic/main_impl.cpp`, `Examples/VortexBasic/MainModule.cpp`, `src/Oxygen/Vortex/Environment/Passes/DistantSkyLightLutPass.{h,cpp}`, `src/Oxygen/Graphics/Direct3D12/Shaders/Vortex/Services/Environment/DistantSkyLightLut.hlsl`, `tools/vortex/AnalyzeRenderDocVortexBasicVolumetric.py`, `tools/vortex/Assert-VortexBasicVolumetricProof.ps1`, new `tools/vortex/Compare-VortexBasicVolumetricTermReports.ps1`, `tools/vortex/README.md`, `design/vortex/PLAN.md`, and this ledger. The DistantSkyLight pass now binds view constants and pads its structured pass constants so the HLSL `float4` light blocks are read at the intended offsets; the temporary shader fallback was removed, so the SkyLight proof measures the LUT path. Syntax/build/test validation passed: `python -m py_compile tools\vortex\AnalyzeRenderDocVortexBasicVolumetric.py`; PowerShell parse for `Compare-VortexBasicVolumetricTermReports.ps1`; `cmake --build out\build-ninja --config Debug --target oxygen-graphics-direct3d12-shaderbake oxygen-vortex oxygen-examples-vortexbasic Oxygen.Vortex.EnvironmentLightingService.Tests Oxygen.Vortex.SceneRendererPublication.Tests --parallel 4`; and `ctest --test-dir out\build-ninja -C Debug -R "EnvironmentLightingService|SceneRendererPublication" --output-on-failure` with 58/58 passing. Local-fog paired captures passed focused validation and directional comparison under `out/build-ninja/analysis/vortex/m04d4-term-proof-localfog`: enabled probe RGB sum `16288.9974`, disabled `13.3762116`, increase `16275.6211884`. SkyLight paired captures passed focused validation and directional comparison under `out/build-ninja/analysis/vortex/m04d4-term-proof-skylight-aligned`: `skylight-enabled.volumetric-report.txt` records `distant_sky_light_lut_value=1224.02563,2076.05298,3352.43164,0`, enabled probe RGB sum `641.44725`, disabled `13.3762116`, increase `628.0710384`; runtime logs record enabled `volumetric_fog_sky_light_injection_executed=true` and disabled `false`. The comparator now requires a positive enabled-minus-disabled increase; the previous suspicious SkyLight absolute-delta run correctly fails with `-0.125578900000001 < 0.001`. Debug-layer audit passed for the SkyLight-enabled volumetric path: `Assert-VortexBasicDebugLayerAudit.ps1` over `skylight-enabled.debug-layer.transcript.log` records `runtime_exit_code=0`, `d3d12_error_count=0`, `dxgi_error_count=0`, `blocking_warning_count=0`, and only the documented shutdown live-factory warning. | `in_progress`; local-fog and SkyLight term-isolated artifact proof is now validated, and the earlier filtered test failures are cleared by rebuilding affected test targets and rerunning the 58-test suite. | VTX-M04D.4 is not complete. Directional-shadow and temporal proof are recorded in newer rows above; UE conservative-depth/pre-exposure temporal refinements, real SkyLight capture/filtering, and city-scale volumetric capture proof remain open. |
| 2026-04-26 | VTX-M04D.4 VortexBasic volumetric term-variant controls | Changed code: `Examples/DemoShell/Runtime/DemoAppContext.h`, `Examples/VortexBasic/main_impl.cpp`, and `Examples/VortexBasic/MainModule.cpp`. Changed docs: `design/vortex/PLAN.md`, this ledger, and `tools/vortex/README.md`. VortexBasic now exposes proof-only controls that leave the default validation scene unchanged: `--volumetric-local-fog false` disables local-fog injection into the volumetric product through `vtx.local_fog.render_into_volumetric_fog`, and `--sky-light-volumetric-scattering 0` authors a SkyLight with no volumetric scattering contribution. Build passed: `cmake --build --preset windows-debug --target oxygen-examples-vortexbasic`. Runtime smoke passed for the default path: `out\build-ninja\bin\Debug\Oxygen.Examples.VortexBasic.exe --frames 3 --fps 30 --with-atmosphere --with-height-fog --with-local-fog --with-volumetric-fog --capture-provider off` exited `0`; log `out/build-ninja/analysis/vortex/m04d4-term-variants-default.log` records `Parsed volumetric-local-fog option = true`, `Parsed sky-light-volumetric-scattering option = 1`, active frames with `volumetric_fog_sky_light_injection_executed=true`, and `volumetric_fog_local_fog_injection_executed=true`. Runtime smoke passed for the disabled-term path: `out\build-ninja\bin\Debug\Oxygen.Examples.VortexBasic.exe --frames 3 --fps 30 --with-atmosphere --with-height-fog --with-local-fog --with-volumetric-fog --volumetric-local-fog false --sky-light-volumetric-scattering 0 --capture-provider off` exited `0`; log `out/build-ninja/analysis/vortex/m04d4-term-variants-disabled.log` records `Parsed volumetric-local-fog option = false`, `Parsed sky-light-volumetric-scattering option = 0`, active frames with `volumetric_fog_sky_light_injection_executed=false`, and `volumetric_fog_local_fog_injection_executed=false` while keeping `integrated_light_scattering_valid=true`. String audit of both logs found `D3D12 ERROR=0`, `D3D12 WARNING=0`, `DXGI ERROR=0`, and `ID3D12=0`. `git diff --check` passed with LF/CRLF warnings only. | `in_progress`; controlled VortexBasic variants now exist for SkyLight and local-fog volumetric term captures. | Superseded by the later 2026-04-26 local-fog/SkyLight term-proof row for those two terms. Separate shadow-term isolation and full UE temporal-policy proof still remain. |
| 2026-04-26 | VTX-M04D.4 focused integrated-volume RenderDoc proof and wrapper correction | Changed tools: `tools/vortex/AnalyzeRenderDocVortexBasicVolumetric.py`, `tools/vortex/Assert-VortexBasicVolumetricProof.ps1`, `tools/vortex/AnalyzeRenderDocVortexBasicCapture.py`, `tools/vortex/AnalyzeRenderDocVortexBasicProducts.py`, `tools/vortex/Assert-VortexBasicRuntimeProof.ps1`, and `tools/vortex/README.md`. Changed docs: `design/vortex/PLAN.md` and this ledger. The focused volumetric analyzer now samples the captured `Vortex.Environment.IntegratedLightScattering` 3D resource on representative slices and asserts nonzero RGB plus valid transmittance alpha. The broader VortexBasic wrapper now matches the VTX-M04D.4 ownership split: Stage 14 local-fog tiled culling still runs, local fog is injected into volumetric fog, and the old Stage 15 local-fog compose draw is expected to be absent in the volumetric proof path. Syntax checks passed: `python -m py_compile tools\vortex\AnalyzeRenderDocVortexBasicVolumetric.py tools\vortex\AnalyzeRenderDocVortexBasicCapture.py tools\vortex\AnalyzeRenderDocVortexBasicProducts.py`. Full wrapper passed: `powershell -NoProfile -File tools\vortex\Run-VortexBasicRuntimeValidation.ps1 -Output out\build-ninja\analysis\vortex\m04d4-volumetric-volume-proof\full-pass2 -Frame 5 -RunFrames 9 -Fps 30 -BuildJobs 4`; validation report `full-pass2.validation.txt` records `analysis_result=success`, `overall_verdict=pass`, `stage14_local_fog_scope_count_match=true`, `stage14_volumetric_fog_dispatch_count_match=true`, `stage15_fog_scene_color_changed=true`, `stage15_local_fog_composed_in_volumetric=true`, `local_fog_tiled_culling_valid=true`, `local_fog_indirect_draw_valid=true`, `integrated_light_scattering_published=true`, `final_present_nonzero=true`, `d3d12_error_count=0`, and `dxgi_error_count=0`. Focused volumetric verifier passed: `powershell -NoProfile -File tools\vortex\Verify-VortexBasicVolumetricProof.ps1 -CapturePath out\build-ninja\analysis\vortex\m04d4-volumetric-volume-proof\full-pass2_capture.rdc -RuntimeLogPath out\build-ninja\analysis\vortex\m04d4-volumetric-volume-proof\full-pass2.stderr.log -CaptureReportPath out\build-ninja\analysis\vortex\m04d4-volumetric-volume-proof\full-pass2.volumetric-report.txt -ValidationReportPath out\build-ninja\analysis\vortex\m04d4-volumetric-volume-proof\full-pass2.volumetric-validation.txt`; focused validation records `overall_verdict=pass`, `integrated_light_scattering_volume_sampled=true`, `integrated_light_scattering_volume_rgb_nonzero=true`, `integrated_light_scattering_volume_alpha_valid=true`, volume dims `240x180x32`, min `0.00151348114,0.00189590454,0.00228691101,0`, max `0.290527344,0.359130859,0.430175781,0.994140625`, and representative slices 0/16/31. String audit of `full-pass2.stderr.log` found `D3D12 ERROR=0`, `D3D12 WARNING=0`, `DXGI ERROR=0`, and `ID3D12=0`. | `in_progress`; focused capture proof now validates the integrated-scattering volume is nonzero and transmittance-bounded, and the broader VortexBasic proof wrapper reflects the current local-fog volumetric ownership. | Not full VTX-M04D.4 closure. This is product-level proof, not term-isolated proof that shadow/local-fog/SkyLight/history each contributes the expected visual delta. Full UE temporal policy, term-isolated artifact proof, real SkyLight capture/filtering, and city-scale capture proof remain open. |
| 2026-04-26 | VTX-M04D.4 first temporal-history/reprojection slice | UE5.7 grounding checked under `F:\Epic Games\UE_5.7`: `Engine/Shaders/Private/VolumetricFog.usf` `LightScatteringCS` for temporal reprojection through `ComputeHistoryVolumeUVFromTranslatedPos`, history UV rejection, `HistoryWeight`, and blending against `LightScatteringHistory`; `Engine/Source/Runtime/Renderer/Private/VolumetricFog.cpp` for `GVolumetricFogTemporalReprojection`, `GVolumetricFogJitter`, `GVolumetricFogHistoryWeight=0.9`, history validity/reset checks, extraction, and previous-volume metadata. Oxygen divergence preserved: Vortex currently has a single integrated-scattering product rather than UE's separate material setup/light-scattering/final-integration products, so this slice retains and reprojects the previous per-view `IntegratedLightScattering` texture after the current integrated value is computed. Changed code: `src/Oxygen/Vortex/Environment/Passes/VolumetricFogPass.{h,cpp}`, `src/Oxygen/Graphics/Direct3D12/Shaders/Vortex/Services/Environment/VolumetricFog.hlsl`, `src/Oxygen/Vortex/Environment/EnvironmentLightingService.{h,cpp}`, `src/Oxygen/Vortex/SceneRenderer/SceneRenderer.{h,cpp}`, and `src/Oxygen/Vortex/Test/EnvironmentLightingService_test.cpp`. Changed docs: `design/vortex/PLAN.md` and this ledger. Implementation: the volumetric pass now owns a per-view history map, retires old history textures safely, enables reprojection only when prior texture dimensions and UE log-grid parameters match, packs the previous SRV and `0.9` history weight into the CPU/HLSL ABI, exposes temporal request/reproject/reset state through generation, Stage 14, and SceneRenderer, and the shader reprojects world-space froxel positions through previous-view bindings before sampling/blending the previous 3D product. Focused build passed: `cmake --build --preset windows-debug --target Oxygen.Vortex.EnvironmentLightingService.Tests`. Focused tests passed: `ctest --preset test-debug -R "Oxygen.Vortex.EnvironmentLightingService" --output-on-failure` with 40/40 tests, including `AuthoredVolumetricFogReusesPerViewTemporalHistoryOnSecondFrame`. Shader validation passed: `cmake --build --preset windows-debug --target oxygen-graphics-direct3d12_shaders`; ShaderBake reported `expanded_requests=184`, `dirty_requests=1`, `clean_requests=183`, `stale_requests=0`, compiled `CS@Vortex/Services/Environment/VolumetricFog.hlsl:VortexVolumetricFogCS`, and wrote 184 modules. Shader catalog tests passed: `ctest --preset test-debug -R "Oxygen.Graphics.Direct3D12.ShaderBakeCatalog" --output-on-failure` with 4/4 tests. Runtime smoke passed: `out\build-ninja\bin\Debug\Oxygen.Examples.VortexBasic.exe --frames 4 --fps 30 --with-atmosphere --with-height-fog --with-local-fog --with-volumetric-fog --capture-provider off` exited `0`; log `out/build-ninja/analysis/vortex/m04d4-volumetric-temporal-smoke.log` records first active volumetric frame with `volumetric_fog_temporal_history_requested=true`, `volumetric_fog_temporal_history_reprojection_executed=false`, `volumetric_fog_temporal_history_reset=true`, and later active frames with `volumetric_fog_temporal_history_reprojection_executed=true` and `volumetric_fog_temporal_history_reset=false`. String audit of that log found `D3D12 ERROR=0`, `D3D12 WARNING=0`, `DXGI ERROR=0`, and `ID3D12=0`. `git diff --check` passed with LF/CRLF warnings only. | `in_progress`; first per-view temporal-history reuse and reprojection slice is implemented and mechanically/runtime-smoke validated. | Not full VTX-M04D.4 closure. Missing UE-complete temporal jitter/history-miss/camera-cut/reset policy, RenderDoc or visual artifact proof that temporal history affects the integrated-scattering product correctly, volumetric shadow/local-fog/SkyLight artifact proof, real SkyLight capture/filtering, and city-scale capture proof. |
| 2026-04-26 | VTX-M04D.4 distant-SkyLight volumetric ambient injection slice | UE5.7 grounding checked under `F:\Epic Games\UE_5.7`: `Engine/Shaders/Private/VolumetricFog.usf` `LightScatteringCS` for SkyLight volumetric scattering gated by `View.SkyLightVolumetricScatteringIntensity`, diffuse SkyLight environment sampling/SH lighting, sky visibility, and additive `LightScattering`; `Engine/Source/Runtime/Renderer/Private/VolumetricFog.cpp` for SkyLight pass parameters, static-lighting exclusion, `SampleSkyLightDiffuseEnvMap`, and `SkyLightUseStaticShadowing`. Oxygen divergence preserved: real SkyLight capture/filtering is still explicitly unavailable, so this slice consumes Oxygen's already-produced atmosphere `DistantSkyLightLut` as the volumetric ambient SkyLight source when an authored SkyLight can affect global illumination. Changed code: `src/Oxygen/Vortex/Environment/Passes/VolumetricFogPass.{h,cpp}`, `src/Oxygen/Graphics/Direct3D12/Shaders/Vortex/Services/Environment/VolumetricFog.hlsl`, `src/Oxygen/Vortex/Environment/EnvironmentLightingService.{h,cpp}`, `src/Oxygen/Vortex/SceneRenderer/SceneRenderer.{h,cpp}`, and `src/Oxygen/Vortex/Test/EnvironmentLightingService_test.cpp`. Changed docs: `design/vortex/PLAN.md` and this ledger. Implementation: the volumetric pass now receives the per-view distant-sky-light LUT SRV, packs SkyLight volumetric scattering/diffuse/tint/intensity controls into the CPU/HLSL ABI, adds the sampled ambient SkyLight term to froxel lighting, and exposes request/execution state through generation, Stage 14, and SceneRenderer state. Build passed: `cmake --build --preset windows-debug --target Oxygen.Vortex.EnvironmentLightingService.Tests` after a standalone rerun. Focused tests passed: `ctest --preset test-debug -R "Oxygen.Vortex.EnvironmentLightingService" --output-on-failure` with 39/39 tests. Shader validation passed: `cmake --build --preset windows-debug --target oxygen-graphics-direct3d12_shaders`; the shader target wrote 184 modules during the first run and a later filtered rerun had no pending shader work. Shader catalog tests passed: `ctest --preset test-debug -R "Oxygen.Graphics.Direct3D12.ShaderBakeCatalog" --output-on-failure` with 4/4 tests. Runtime smoke passed: `out\build-ninja\bin\Debug\Oxygen.Examples.VortexBasic.exe --frames 3 --fps 30 --with-atmosphere --with-height-fog --with-local-fog --with-volumetric-fog --capture-provider off` exited `0`; log `out/build-ninja/analysis/vortex/m04d4-volumetric-skylight-smoke.log` records active frames with `volumetric_fog_requested=true`, `volumetric_fog_executed=true`, `integrated_light_scattering_valid=true`, `volumetric_fog_sky_light_injection_requested=true`, and `volumetric_fog_sky_light_injection_executed=true`. String audit of that log found `D3D12 ERROR=0`, `D3D12 WARNING=0`, `DXGI ERROR=0`, and `ID3D12=0`. | `in_progress`; first Oxygen distant-SkyLight ambient injection into volumetric fog is implemented and mechanically/runtime-smoke validated. | Not full VTX-M04D.4 closure. Missing capture/artifact proof for the SkyLight term in the integrated-scattering product, temporal history/reprojection, volumetric shadow/local-fog artifact proof, real SkyLight capture/filtering, and city-scale capture proof. |
| 2026-04-26 | VTX-M04D.4 spatial height-fog media density slice | UE5.7 grounding checked under `F:\Epic Games\UE_5.7`: `Engine/Shaders/Private/VolumetricFog.usf` `MaterialSetupCS` for primary/secondary exponential height-fog density at froxel world position, `MatchHeightFogFactor = .5f`, global extinction scale, albedo-scaled scattering, and local-fog additive composition; `Engine/Shaders/Private/HeightFogCommon.ush` for the same `exp2(-falloff * (WorldPosition.z - FogHeight))` height-fog convention. Changed code: `src/Oxygen/Vortex/Environment/Passes/VolumetricFogPass.{h,cpp}`, `src/Oxygen/Graphics/Direct3D12/Shaders/Vortex/Services/Environment/VolumetricFog.hlsl`, `src/Oxygen/Vortex/Environment/EnvironmentLightingService.{h,cpp}`, `src/Oxygen/Vortex/SceneRenderer/SceneRenderer.{h,cpp}`, and `src/Oxygen/Vortex/Test/EnvironmentLightingService_test.cpp`. Changed docs: `design/vortex/PLAN.md` and this ledger. Implementation: volumetric pass constants now carry primary and secondary height-fog density/falloff/height offsets, the shader evaluates spatial density per froxel with UE-shaped `exp2` height falloff and the 0.5 height-fog matching factor, then applies volumetric extinction scale and near fade before adding local-fog extinction. Generation/Stage-14/SceneRenderer state now exposes whether height-fog media was requested and executed. Build passed: `cmake --build --preset windows-debug --target Oxygen.Vortex.EnvironmentLightingService.Tests`. Focused tests passed: `ctest --preset test-debug -R "Oxygen.Vortex.EnvironmentLightingService" --output-on-failure` with 39/39 tests. Shader validation passed: `cmake --build --preset windows-debug --target oxygen-graphics-direct3d12_shaders`; ShaderBake reported `expanded_requests=184`, `dirty_requests=1`, `clean_requests=183`, `stale_requests=0`, and wrote 184 modules. Shader catalog tests passed: `ctest --preset test-debug -R "Oxygen.Graphics.Direct3D12.ShaderBakeCatalog" --output-on-failure` with 4/4 tests. Runtime smoke passed: `out\build-ninja\bin\Debug\Oxygen.Examples.VortexBasic.exe --frames 3 --fps 30 --with-atmosphere --with-height-fog --with-local-fog --with-volumetric-fog --capture-provider off` exited `0`. `git diff --check` passed with LF/CRLF warnings only. | `in_progress`; first UE5.7-shaped spatial height-fog media-density path is implemented and mechanically/runtime-smoke validated. | Not full VTX-M04D.4 closure. Missing capture proof for height-fog media changes in the integrated-scattering product, plus temporal history/reprojection, volumetric shadow/local-fog/SkyLight artifact proof, real SkyLight capture/filtering, and city-scale capture proof. |
| 2026-04-26 | VTX-M04D.4 local-fog participating-media injection slice | UE5.7 grounding checked under `F:\Epic Games\UE_5.7`: `Engine/Shaders/Private/VolumetricFog.usf` for `USE_LOCAL_FOG_VOLUMES`, froxel front/back distance gating, local-fog tile lookup, sphere fade, height/radial extinction combination, and `LFVMaxDensityIntoVolumetricFog`; `Engine/Shaders/Private/LocalFogVolumes/LocalFogVolumeCommon.ush` for packed local-fog instance semantics. Oxygen divergence preserved: world units stay meters, so the UE centimeter conversion is not applied, and the injection consumes Oxygen's validated Stage-14 tiled local-fog products instead of adding a second culling path. Changed code: `src/Oxygen/Vortex/Renderer.{h,cpp}`, `src/Oxygen/Vortex/Environment/EnvironmentLightingService.{h,cpp}`, `src/Oxygen/Vortex/Environment/Passes/VolumetricFogPass.{h,cpp}`, `src/Oxygen/Vortex/SceneRenderer/SceneRenderer.{h,cpp}`, `src/Oxygen/Graphics/Direct3D12/Shaders/Vortex/Services/Environment/VolumetricFog.hlsl`, and `src/Oxygen/Vortex/Test/EnvironmentLightingService_test.cpp`. Changed docs: `design/vortex/PLAN.md` and this ledger. Implementation: `PublishEnvironmentBindings` can receive `SceneTextures`, prepares and tiled-culls local fog once per view/frame before the volumetric pass records, passes the validated local-fog instance/tile products to `VolumetricFogPass`, publishes local-fog injection observability through generation/Stage-14/SceneRenderer state, and skips the Stage-15 local-fog compose draw when local fog was injected into volumetric fog. The volumetric shader now includes `LocalFogVolumeCommon.hlsli`, evaluates UE-shaped local-fog froxel media from tile data, applies the global start-distance soft scale, clamps with `vtx.local_fog.max_density_into_volumetric_fog`, and adds local-fog extinction/scattering/emissive to the integrated-scattering product. Build passed: `cmake --build --preset windows-debug --target Oxygen.Vortex.EnvironmentLightingService.Tests`. Focused tests passed: `ctest --preset test-debug -R "Oxygen.Vortex.EnvironmentLightingService" --output-on-failure` with 39/39 tests. Shader validation passed: `cmake --build --preset windows-debug --target oxygen-graphics-direct3d12_shaders`; ShaderBake reported `expanded_requests=184`, `dirty_requests=1`, `clean_requests=183`, `stale_requests=0`, and wrote 184 modules. Shader catalog tests passed: `ctest --preset test-debug -R "Oxygen.Graphics.Direct3D12.ShaderBakeCatalog" --output-on-failure` with 4/4 tests. Runtime smoke passed: `out\build-ninja\bin\Debug\Oxygen.Examples.VortexBasic.exe --frames 3 --fps 30 --with-atmosphere --with-height-fog --with-local-fog --with-volumetric-fog --capture-provider off` exited `0`. `git diff --check` passed with LF/CRLF warnings only. | `in_progress`; first local-fog participating-media injection into volumetric fog is implemented and mechanically validated against the existing local-fog tiled products. | Not full VTX-M04D.4 closure. Missing runtime/capture proof that local-fog injection changes the integrated-scattering product as intended, plus temporal history/reprojection, volumetric shadow/local-fog/SkyLight artifact proof, real SkyLight capture/filtering, and city-scale capture proof. |
| 2026-04-26 | VTX-M04D.4 city directional CSM hydration fix | Changed code: `Examples/DemoShell/Services/SceneLoaderService.cpp` and `Examples/DemoShell/Test/SceneLoaderService_phase4_test.cpp`. Changed docs: `design/vortex/PLAN.md`, `design/vortex/plan/VTX-M04D.4-directional-csm-blocker.md`, and this ledger. Root cause found for `CityEnvironmentValidation` showing no projected shadows while smaller scenes did: the cooked directional-light record carried city-scale manual CSM settings, but `SceneLoaderService` only hydrated cascade count, cascade distances, and distribution exponent. Runtime lights therefore lost authored `split_mode`, `max_shadow_distance`, `transition_fraction`, and `distance_fadeout_fraction`, falling back to the default generated 160 m CSM range that is adequate for small proof scenes but not the meter-scale city. The loader now copies those fields and canonicalizes the runtime `DirectionalLight` CSM settings. Regression coverage builds a cooked directional-light component with 4.2 km manual CSM values and verifies shadow casting, sun metadata, intensity, split mode, max distance, all manual cascade distances, distribution exponent, transition fraction, and fadeout fraction survive scene hydration. Build passed: `cmake --build out\build-ninja --target Oxygen.Examples.DemoShell.SceneLoaderServicePhase4.Tests --config Debug`; `cmake --build out\build-ninja --target Oxygen.Examples.RenderScene.exe --config Debug`. Focused test passed: `out\build-ninja\bin\Debug\Oxygen.Examples.DemoShell.SceneLoaderServicePhase4.Tests.exe --gtest_filter=SceneLoaderServicePhase4Test.BuildSceneAsyncHydratesDirectionalLightCsmSettings`. Suite test passed: `ctest --preset test-debug -R "Oxygen.Examples.DemoShell.SceneLoaderServicePhase4" --output-on-failure` with 7/7 tests. Runtime smoke passed: `out\build-ninja\bin\Debug\Oxygen.Examples.RenderScene.exe --frames 3 --fps 30 --capture-provider=off` exited `0`. `git diff --check` passed with LF/CRLF warnings only. User visual confirmation recorded on 2026-04-26: "The scene has shadows now." | `validated` for the city-scale directional CSM hydration defect; implementation, docs/status, focused tests, RenderScene smoke, and user visual confirmation are recorded. | Full VTX-M04D.4 volumetric-fog parity remains open for media/local/sky-light injection, temporal history/reprojection, volumetric shadow artifact proof, and city-scale capture proof. |
| 2026-04-26 | VTX-M04D.4 volumetric directional CSM injection and log-froxel slice | UE5.7 grounding checked under `F:\Epic Games\UE_5.7`: `Engine/Shaders/Private/VolumetricFog.usf` for `ComputeDepthFromZSlice`, world-position reconstruction, and directional light shadow factor multiplication; `Engine/Shaders/Private/Froxel/Froxel.ush` for logarithmic froxel depth mapping; `Engine/Source/Runtime/Renderer/Private/VolumetricFog.cpp` and `VolumetricFogShared.h` for `GVolumetricFogDepthDistributionScale=32`, `GetVolumetricFogGridZParams`, and directional shadowing setup; and `Engine/Source/Runtime/RenderCore/Public/RenderUtils.h` for `CalculateGridZParams`. Changed code/tools: `src/Oxygen/Vortex/Environment/Passes/VolumetricFogPass.{h,cpp}`, `src/Oxygen/Graphics/Direct3D12/Shaders/Vortex/Services/Environment/VolumetricFog.hlsl`, `src/Oxygen/Graphics/Direct3D12/Shaders/Vortex/Services/Environment/Fog.hlsl`, `src/Oxygen/Graphics/Direct3D12/Shaders/Vortex/Services/Shadows/DirectionalShadowCommon.hlsli`, `src/Oxygen/Vortex/Environment/EnvironmentLightingService.{h,cpp}`, `src/Oxygen/Vortex/Types/EnvironmentStaticData.h`, `src/Oxygen/Graphics/Direct3D12/Shaders/Vortex/Contracts/Environment/EnvironmentStaticData.hlsli`, `src/Oxygen/Vortex/Test/EnvironmentLightingService_test.cpp`, `tools/vortex/AnalyzeRenderDocVortexBasicVolumetric.py`, `tools/vortex/Assert-VortexBasicVolumetricProof.ps1`, `tools/vortex/ProbeRenderDocEnvironmentStaticData.py`, and `tools/vortex/README.md`. Changed docs: `design/vortex/PLAN.md`, `design/vortex/IMPLEMENTATION_STATUS.md`, and `design/vortex/lld/environment-service.md`. Implementation: the volumetric pass now requires and binds `ViewConstants`, computes UE-shaped log-depth grid params, reconstructs per-froxel world position from projection depth, evaluates the directional phase per froxel instead of using a fixed representative view direction, and samples the validated conventional directional CSM through a volumetric helper that reuses cascade selection/transition/fade without surface normal bias. The `EnvironmentStaticData` ABI now publishes the same log-depth grid params in a 672-byte payload, and Stage 15 fog maps ray depth back through those params when sampling `IntegratedLightScattering`. Build passed: `cmake --build --preset windows-debug --target Oxygen.Vortex.EnvironmentLightingService`. ShaderBake passed: `cmake --build --preset windows-debug --target oxygen-graphics-direct3d12_shaders`; final ABI run reported `expanded_requests=184`, `dirty_requests=126`, `clean_requests=58`, `stale_requests=0`, and wrote 184 modules. Syntax checks passed: `python -m py_compile tools\vortex\AnalyzeRenderDocVortexBasicVolumetric.py tools\vortex\ProbeRenderDocEnvironmentStaticData.py`. Focused tests passed: `ctest --preset test-debug -R "Oxygen.Vortex.EnvironmentLightingService" --output-on-failure` with 38/38 tests. Runtime smoke passed: `out\build-ninja\bin\Debug\Oxygen.Examples.VortexBasic.exe --frames 5 --fps 30 --with-atmosphere --with-height-fog --with-local-fog --with-volumetric-fog --capture-provider off` exited `0`; log `out/build-ninja/analysis/vortex/m04d4-volumetric-shadow-smoke.log` records active frames with `volumetric_fog_requested=true`, `volumetric_fog_executed=true`, `integrated_light_scattering_valid=true`, `volumetric_fog_grid=240x180x32`, `volumetric_fog_view_constants_bound=true`, `volumetric_fog_ue_log_depth_distribution=true`, and `volumetric_fog_directional_shadowed_light_requested=true`. D3D12 debug-layer string audit on that log found debug layer enabled and no `D3D12 ERROR`, `D3D12 WARNING`, `ID3D12`, or blocking validation messages. | `in_progress`; first UE5.7-shaped log-froxel depth, matching Stage-15 log-depth sampling, and primary directional CSM volumetric shadow sampling are implemented and mechanically validated. | Not full VTX-M04D.4 closure. Missing RenderDoc/visual artifact proof that volumetric shadows attenuate the integrated-scattering product as intended, plus remaining local-fog participating-media injection, sky/local light injection, temporal history/reprojection, and city-scale capture proof. |
| 2026-04-25 | VTX-M04D.4 directional CSM projected-shadow closure | Changed code/tools: `Examples/VortexBasic/MainModule.cpp`, `Examples/VortexBasic/main_impl.cpp`, `src/Oxygen/Graphics/Direct3D12/Shaders/EngineShaderCatalog.h`, `src/Oxygen/Graphics/Direct3D12/Shaders/Vortex/Services/Shadows/DirectionalShadowCommon.hlsli`, `src/Oxygen/Graphics/Direct3D12/Shaders/Vortex/Stages/BasePass/BasePassDebugView.hlsl`, `src/Oxygen/Vortex/ShaderDebugMode.h`, `src/Oxygen/Vortex/SceneRenderer/SceneRenderer.cpp`, `src/Oxygen/Vortex/Shadows/Internal/CascadeShadowSetup.cpp`, `src/Oxygen/Vortex/Shadows/Types/ShadowCascadeBinding.h`, `src/Oxygen/Vortex/Types/ShadowFrameBindings.h`, `Examples/DemoShell/Services/RenderingSettingsService.cpp`, `Examples/DemoShell/UI/RenderingPanel.cpp`, `tools/vortex/ProbeRenderDocDirectionalCsm.py`, and `tools/vortex/ProbeRenderDocStageOutputs.py`. Changed docs: `design/vortex/plan/VTX-M04D.4-directional-csm-blocker.md`, `design/vortex/lld/shadow-service.md`, and this ledger. Corrective implementation: CPU/HLSL cascade stride is now 112 bytes on both sides, reversed-Z camera frustum extraction now uses near/far NDC z consistent with Oxygen reversed-Z, the shadow frame publishes `light_direction_to_source` for debug receiver consumers, `directional-shadow-mask` is a deferred conventional CSM debug route/catalog variant, and VortexBasic uses a raised sun plus a 60x60 floor receiver for clearer close-caster projected-shadow proof. Build passed: `cmake --build --preset windows-debug --target oxygen-examples-vortexbasic Oxygen.Graphics.Direct3D12.ShaderBakeCatalog.Tests Oxygen.Vortex.SceneRendererDeferredCore.Tests Oxygen.Vortex.ShadowService.Tests`; ShaderBake reported `expanded_requests=184`, `dirty_requests=8`, `clean_requests=176`, `stale_requests=1`, removed the stale BasePass `DEBUG_VIRTUAL_SHADOW_MASK` artifact, and wrote 184 modules. Focused tests passed: `ctest --preset test-debug -R "(Oxygen\.Graphics\.Direct3D12\.ShaderBakeCatalog\.Tests|Oxygen\.Vortex\.(SceneRendererDeferredCore|ShadowService)\.Tests)" --output-on-failure`, with ShadowService 5/5, ShaderBakeCatalog 4/4, and SceneRendererDeferredCore 32/32. Normal RenderDoc capture `out/build-ninja/analysis/vortex/csm-current/vortexbasic-csm-final-proof-renamed_capture.rdc` probed successfully: `stage8_draw_count=8`, cascades 0..3 all `wrote_non_clear=true` with maxima `0.441091061`, `0.510262966`, `0.593575954`, and `0.368894488`; Stage 12 binds `stage12_pixel_ro_8=Vortex.DirectionalShadowSurface`; receiver samples include lit points (`0,0.5,0` visibility `1.0`, `0,3,0` visibility `1.0`, and `0.5,1.5,0` visibility `1.0`) and shadowed points (`0,1,0`, `0,1.5,0`, `0,2,0`, `0,2.5,0`, and `-0.5,1.5,0` visibility `0.0`). The `directional-shadow-mask` capture `out/build-ninja/analysis/vortex/csm-current/vortexbasic-csm-final-proof-renamed-shadowmask_capture.rdc` probed successfully with `debug_directional_shadow_mask_present=true`, `debug_directional_shadow_mask_min=[0.0, 0.0, 0.0, 1.0]`, and `debug_directional_shadow_mask_max=[1.0, 1.0, 1.0, 1.0]`. Debug-layer audit passed under `cdb -g -G` via `tools/vortex/Assert-VortexBasicDebugLayerAudit.ps1`; `out/build-ninja/analysis/vortex/csm-current/vortexbasic-csm-final-proof-renamed.debug-layer.report.txt` records `runtime_exit_code=0`, `debugger_break_detected=false`, `d3d12_error_count=0`, `dxgi_error_count=0`, `blocking_warning_count=0`, and one accepted shutdown `DXGI WARNING: Live IDXGIFactory`. User visual confirmation recorded in chat: "I see shadows." | `validated` for the conventional directional CSM projected-shadow blocker. Implementation, docs/status, build, ShaderBake/catalog, focused tests, RenderDoc receiver proof, debug-mask proof, D3D12 debug-layer audit, and user visual confirmation are all recorded. | This does not close full VTX-M04D.4 volumetric-fog parity. Remaining VTX-M04D.4 work: depth-aware froxel distribution, shadowed/local/sky-light injection, local-fog participating-media injection, temporal history/reprojection, and artifact-quality proof. |
| 2026-04-25 | VTX-M04D.4 directional CSM reversed-Z and bindless correction | Changed code: `src/Oxygen/Vortex/Shadows/Internal/CascadeShadowSetup.cpp`, `src/Oxygen/Vortex/Shadows/Internal/ConventionalShadowTargetAllocator.cpp`, `src/Oxygen/Vortex/Shadows/Passes/ShadowDepthPass.cpp`, `src/Oxygen/Graphics/Direct3D12/Shaders/Vortex/Services/Shadows/DirectionalShadowDepth.hlsl`, and `src/Oxygen/Graphics/Direct3D12/Shaders/Vortex/Services/Shadows/DirectionalShadowCommon.hlsli`. Changed tools/docs: `tools/vortex/ProbeRenderDocDirectionalCsm.py`, `design/vortex/plan/VTX-M04D.4-directional-csm-blocker.md`, `design/vortex/lld/shadow-service.md`, and this ledger. Corrective implementation: conventional directional CSM now uses Oxygen reversed-Z clear/projection/depth-compare/receiver-compare conventions, per-cascade shadow pass constants carry direct draw metadata/current-worlds/instance-data slots, the shadow-depth shader no longer depends on mutable view-frame draw bindings, and the receiver no longer rejects valid heap handles using stale generated domain ranges. Build passed: `cmake --build --preset windows-debug --target oxygen-examples-vortexbasic Oxygen.Vortex.ShadowService.Tests Oxygen.Vortex.SceneRendererDeferredCore.Tests`; ShaderBake reported `expanded_requests=183`, `dirty_requests=2`, `clean_requests=181`, `stale_requests=0`, and wrote 183 modules after the final receiver change. Focused tests passed: `ctest --preset test-debug -R "Oxygen\.Vortex\.(ShadowService\|SceneRendererDeferredCore)\.Tests" --output-on-failure`, with ShadowService 5/5 and SceneRendererDeferredCore 32/32. Runtime smoke passed: `out\build-ninja\bin\Debug\Oxygen.Examples.VortexBasic.exe -v=-1 --frames 2 --fps 30 --with-atmosphere true --with-height-fog true --with-local-fog true --with-volumetric-fog true --capture-provider off` exited `0`. RenderDoc capture: `out/build-ninja/analysis/vortex/csm-current/vortexbasic-csm-validonly_capture.rdc`; focused probe records `stage8_draw_count=8`, `stage8_clear_count=4`, `cascade_2_max=0.088183910`, `cascade_2_wrote_non_clear=true`, `cascade_3_max=0.343878120`, `cascade_3_wrote_non_clear=true`, and Stage-12 light constants with `shadow_info.x=4`. D3D12 debug-layer audit under `cdb.exe` passed via `tools/vortex/Assert-VortexBasicDebugLayerAudit.ps1`; report `out/build-ninja/analysis/vortex/csm-current/vortexbasic-csm-validonly.debug-layer.report.txt` records `runtime_exit_code=0`, `d3d12_error_count=0`, `dxgi_error_count=0`, `blocking_warning_count=0`, and only the accepted shutdown `DXGI WARNING: Live IDXGIFactory`. | Historical `in_progress` slice; Stage 8 no longer stayed all-clear, later cascades wrote non-clear directional shadow depth, the directional light requested shadowing, focused CPU/shader/runtime checks passed, and the D3D12 debug layer reported no blocking errors or warnings. | Superseded by the projected-shadow closure row above, which records all-cascade writes, Stage-12 receiver attenuation proof, debug-mask proof, and user visual confirmation. |
| 2026-04-25 | VTX-M04D.4 visual-gate correction after no projected shadows | User visual check reported no projected shadows after the first CSM implementation slice, so this work remains `in_progress`. Corrective code changed `Examples/VortexBasic/MainModule.cpp` to explicitly author the proof-scene directional light with `Common().casts_shadows = true` and explicitly set both `Cube` and `Floor` scene flags to `kCastsShadows=true` and `kReceivesShadows=true` instead of relying on inherited defaults. Build passed: `cmake --build --preset windows-debug --target oxygen-examples-vortexbasic Oxygen.Vortex.SceneRendererDeferredCore.Tests Oxygen.Vortex.ShadowService.Tests`. Focused tests passed: `ctest --preset test-debug -R "Oxygen\.Vortex\.(SceneRendererDeferredCore\|ShadowService)\.Tests" --output-on-failure`, with ShadowService 5/5 and SceneRendererDeferredCore 32/32. `git diff --check` passed with LF/CRLF warnings only. | `in_progress`; light and proof-scene geometry shadow participation are now explicit and mechanically validated. | Projected shadows are still not visually confirmed. Need runtime/capture receiver-pixel attenuation proof and user visual confirmation before closure or before any shadowed volumetric-light injection claim. |
| 2026-04-25 | VTX-M04D.4 directional CSM implementation slice | Changed code: `src/Oxygen/Vortex/Types/FrameLightSelection.h`, `src/Oxygen/Vortex/SceneRenderer/SceneRenderer.cpp`, `src/Oxygen/Vortex/Lighting/Types/DirectionalLightForwardData.h`, `src/Oxygen/Vortex/Shadows/ShadowService.cpp`, `src/Oxygen/Vortex/Shadows/Internal/CascadeShadowSetup.cpp`, `src/Oxygen/Graphics/Direct3D12/Shaders/Vortex/Services/Shadows/DirectionalShadowCommon.hlsli`, `Examples/VortexBasic/MainModule.cpp`, `src/Oxygen/Vortex/Test/SceneRendererDeferredCore_test.cpp`, and `src/Oxygen/Vortex/Test/ShadowService_test.cpp`. Changed docs: `design/vortex/plan/VTX-M04D.4-directional-csm-blocker.md`, `design/vortex/lld/shadow-service.md`, and `design/vortex/IMPLEMENTATION_STATUS.md`. UE5.7 grounding checked: `Engine/Source/Runtime/Engine/Private/Components/DirectionalLightComponent.cpp` for `ComputeAccumulatedScale`, generated split distances, transition fraction, and last-cascade fade behavior; `Engine/Shaders/Private/ForwardShadowingCommon.ush` for cascade selection/world-to-shadow sampling; `Engine/Shaders/Private/ShadowFilteringCommon.ush` for PCF depth comparison/transition behavior; plus the Renderer shadow files already listed in the blocker plan. Build passed after correcting the example target name: `cmake --build --preset windows-debug --target Oxygen.Vortex.ShadowService.Tests Oxygen.Vortex.SceneRendererDeferredCore.Tests Oxygen.Graphics.Direct3D12.ShaderBakeCatalog.Tests oxygen-examples-vortexbasic`. The build's ShaderBake step reported `expanded_requests=183`, `dirty_requests=2`, `clean_requests=181`, `stale_requests=0`, and wrote 183 modules. Tests passed: `ctest --preset test-debug -R "(Oxygen\.Vortex\.(ShadowService\|SceneRendererDeferredCore)\.Tests\|Oxygen\.Graphics\.Direct3D12\.ShaderBakeCatalog\.Tests)" --output-on-failure`, with ShadowService 5/5, SceneRendererDeferredCore 32/32, and ShaderBakeCatalog 4/4. Runtime smoke passed on the real D3D12 path: `out\build-ninja\bin\Debug\Oxygen.Examples.VortexBasic.exe -v=-1 --frames 2 --fps 30 --with-atmosphere true --with-height-fog true --with-local-fog true --with-volumetric-fog true --capture-provider off` exited `0`. Headless smoke is not used as evidence: the equivalent `--headless true` run advanced through `Frame(seq:2)` but exited `-1073741819` without logged error detail. | `in_progress`; authored directional CSM settings now reach Stage 8/12 metadata, shader sampling consumes per-cascade transition/fade/bias metadata, focused CPU/shader validation passes, and VortexBasic D3D12 startup/frame/shutdown proof exists. | Not directional CSM closure. Missing RenderDoc/runtime proof that Stage 8 cascade depths attenuate Stage 12 receiver pixels, and missing user visual confirmation of projected shadows. VTX-M04D.4 shadowed volumetric-light injection remains blocked. |
| 2026-04-25 | VTX-M04D.4 directional CSM blocker scope correction | UE5.7 references inspected under `F:\Epic Games\UE_5.7`: `Engine/Source/Runtime/Renderer/Private/ShadowSetup.cpp`, `ShadowRendering.cpp`, `ShadowDepthRendering.cpp`, `Engine/Shaders/Private/ForwardShadowingCommon.ush`, `ShadowFilteringCommon.ush`, and `DeferredLightPixelShaders.usf`. Vortex references inspected: `src/Oxygen/Vortex/Shadows/*`, `src/Oxygen/Vortex/Lighting/Passes/DeferredLightPass.cpp`, `src/Oxygen/Graphics/Direct3D12/Shaders/Vortex/Services/Shadows/DirectionalShadowCommon.hlsli`, and `DeferredLightDirectional.hlsl`. Changed docs: `design/vortex/plan/VTX-M04D.4-directional-csm-blocker.md`, `design/vortex/plan/README.md`, `design/vortex/PLAN.md`, and `design/vortex/IMPLEMENTATION_STATUS.md`. | `in_progress`; scope corrected. Conventional directional CSM projected-shadow proof is now an explicit blocker for VTX-M04D.4 shadowed-light injection. | Docs-only correction. No code, build, ShaderBake, or runtime/capture validation in this row. |
| 2026-04-25 | VTX-M04D.3 local-fog runtime/capture parity proof | Changed tools/docs: `tools/vortex/AnalyzeRenderDocVortexBasicProducts.py`, `tools/vortex/Assert-VortexBasicRuntimeProof.ps1`, `design/vortex/plan/VTX-M04D.3-local-fog-volume-parity.md`, `design/vortex/PLAN.md`, and `design/vortex/IMPLEMENTATION_STATUS.md`. Syntax checks passed: `python -m py_compile tools/vortex/AnalyzeRenderDocVortexBasicProducts.py tools/vortex/ProbeRenderDocLocalFogState.py`. `git diff --check` passed with LF/CRLF warnings only. Full VortexBasic wrapper passed: `powershell -NoProfile -File tools/vortex/Run-VortexBasicRuntimeValidation.ps1 -Output out/build-ninja/analysis/vortex/local-fog-proof-gates/full-pass -Frame 5 -RunFrames 9 -Fps 30 -BuildJobs 4`; validation records `analysis_result=success`, `overall_verdict=pass`, `runtime_exit_code=0`, `local_fog_volume_instance_count=1`, `screen_hzb_published=true`, `local_fog_hzb_consumed=true`, `local_fog_tiled_culling_valid=true`, `local_fog_indirect_draw_valid=true`, `local_fog_scene_color_changed=true`, `local_fog_far_depth_unchanged=true`, `stage15_local_fog_scene_color_changed=true`, `stage15_local_fog_far_depth_unchanged=true`, `stage15_local_fog_scope_count_match=true`, `stage15_local_fog_draw_count_match=true`, `stage15_runtime_stage_order_match=true`, `final_present_nonzero=true`, `blocking_warning_count=0`, `d3d12_error_count=0`, and `dxgi_error_count=0`; the single accepted warning is the documented shutdown `DXGI WARNING: Live IDXGIFactory`. Product report records `stage15_local_fog_scene_color_delta_max=0.051270`, `stage15_local_fog_far_depth_delta_max=0.000000`, `stage15_local_fog_far_depth_sample_count=14`, `local_fog_indirect_draw_args=6,180,0,0`, and `local_fog_indirect_draw_instance_count=180`. Focused probe passed: `powershell -NoProfile -File tools/shadows/Invoke-RenderDocUiAnalysis.ps1 -CapturePath out/build-ninja/analysis/vortex/local-fog-proof-gates/full-pass_capture.rdc -UiScriptPath tools/vortex/ProbeRenderDocLocalFogState.py -PassName VortexLocalFogStateProbe -ReportPath out/build-ninja/analysis/vortex/local-fog-proof-gates/full-pass.local-fog-state.txt`; probe records `stage14_dispatch_event=323`, `stage15_local_fog_event=411`, `tile_draw_args_values=6,180,0,0`, and `tile_draw_instance_count=180`. Prior same-slice validation remains applicable: focused build/test `Oxygen.Vortex.EnvironmentLightingService.Tests` 38/38, ShaderBake `expanded_requests=183`, `dirty_requests=3`, `clean_requests=180`, `stale_requests=0`, 183 modules written, and ShaderBakeCatalog 4/4. | `validated`; VTX-M04D.3 analytical local-fog volume parity is closed for authoring, CPU/GPU packing, sorting/capping, Stage-14 HZB-backed tiled culling, UE-shaped indirect draw, Stage-15 compose, SceneColor contribution, far-depth/sky exclusion, stage ordering, and focused runtime/capture proof. | Local-fog participating-media injection into volumetric fog is not claimed here and remains VTX-M04D.4 scope. |
| 2026-04-25 | VTX-M04D.3 UE-shaped local-fog tiled draw slice | UE5.7 references checked under `F:\Epic Games\UE_5.7`: `Engine/Source/Runtime/Renderer/Private/LocalFogVolumeRendering.cpp`, `Engine/Shaders/Private/LocalFogVolumes/LocalFogVolumeTiledCulling.usf`, and `Engine/Shaders/Private/LocalFogVolumes/LocalFogVolumeSplat.usf`. Changed code/tools/docs: `src/Oxygen/Vortex/Environment/Internal/LocalFogVolumeState.h`, `src/Oxygen/Vortex/Environment/Passes/LocalFogVolumeTiledCullingPass.{h,cpp}`, `src/Oxygen/Vortex/Environment/Passes/LocalFogVolumeComposePass.cpp`, `src/Oxygen/Graphics/Direct3D12/Shaders/Vortex/Services/Environment/LocalFogVolumeCommon.hlsli`, `src/Oxygen/Graphics/Direct3D12/Shaders/Vortex/Services/Environment/LocalFogVolumeTiledCulling.hlsl`, `src/Oxygen/Vortex/Test/Fakes/Graphics.h`, `src/Oxygen/Vortex/Test/EnvironmentLightingService_test.cpp`, `tools/vortex/AnalyzeRenderDocVortexBasicProducts.py`, `tools/vortex/ProbeRenderDocLocalFogState.py`, `design/vortex/plan/VTX-M04D.3-local-fog-volume-parity.md`, `design/vortex/PLAN.md`, and `design/vortex/IMPLEMENTATION_STATUS.md`. Focused build passed: `cmake --build --preset windows-debug --target Oxygen.Vortex.EnvironmentLightingService.Tests`. Focused tests passed: `ctest --preset test-debug -R "Oxygen.Vortex.EnvironmentLightingService" --output-on-failure` with 38/38 tests. Shader validation passed: `cmake --build --preset windows-debug --target oxygen-graphics-direct3d12`; ShaderBake reported `expanded_requests=183`, `dirty_requests=3`, `clean_requests=180`, `stale_requests=0`, and wrote 183 modules. Shader catalog tests passed: `ctest --preset test-debug -R "Oxygen.Graphics.Direct3D12.ShaderBakeCatalog" --output-on-failure` with 4/4 tests. Syntax checks passed: `python -m py_compile tools/vortex/AnalyzeRenderDocVortexBasicProducts.py tools/vortex/ProbeRenderDocLocalFogState.py`. `git diff --check` passed with LF/CRLF warnings only. | `in_progress`; Stage 14 now initializes one UE-shaped draw-indirect packet, increments the packet instance count per occupied tile, and Stage 15 executes one indirect draw without a separate count buffer. The product analyzer now checks `LocalFogTileDrawArgs={6, instance_count, 0, 0}` and requires a nonzero captured instance count when runtime capture proof is refreshed. | Not a UE5.7 local-fog parity closure. Fresh runtime/capture proof for the changed shader path is still required, along with analytical integral/lighting proof, sky-depth exclusion proof, and VTX-M04D.4 local-fog volumetric injection handoff. |
| 2026-04-25 | VTX-M04D.3 local-fog authoring and cap parity slice | UE5.7 references checked under `F:\Epic Games\UE_5.7`: `Engine/Source/Runtime/Engine/Classes/Components/LocalFogVolumeComponent.h`, `Engine/Source/Runtime/Engine/Public/LocalFogVolumeSceneProxy.h`, `Engine/Source/Runtime/Engine/Private/LocalFogVolumeSceneProxy.cpp`, `Engine/Source/Runtime/Renderer/Private/LocalFogVolumeRendering.{h,cpp}`, `Engine/Shaders/Private/LocalFogVolumes/LocalFogVolumeCommon.ush`, `Engine/Shaders/Private/LocalFogVolumes/LocalFogVolumeTiledCulling.usf`, `Engine/Shaders/Private/LocalFogVolumes/LocalFogVolumeSplat.usf`, `Engine/Shaders/Private/HeightFogPixelShader.usf`, and `Engine/Shaders/Private/VolumetricFog.usf`. Changed docs/code/tests: `design/vortex/plan/VTX-M04D.3-local-fog-volume-parity.md`, `design/vortex/plan/README.md`, `design/vortex/PLAN.md`, `design/vortex/IMPLEMENTATION_STATUS.md`, `src/Oxygen/Scene/Environment/LocalFogVolume.h`, `src/Oxygen/Scene/Test/EnvironmentComponents_test.cpp`, `src/Oxygen/Vortex/Environment/Internal/LocalFogVolumeState.cpp`, `src/Oxygen/Vortex/Renderer.cpp`, and `src/Oxygen/Vortex/Test/EnvironmentLightingService_test.cpp`. Focused build passed: `cmake --build --preset windows-debug --target Oxygen.Scene.EnvironmentComponents.Tests Oxygen.Vortex.EnvironmentLightingService.Tests`. Focused tests passed: `ctest --preset test-debug -R "Oxygen\.(Scene\.EnvironmentComponents\|Vortex\.EnvironmentLightingService)" --output-on-failure` with `Oxygen.Scene.EnvironmentComponents.Tests` 5/5 and `Oxygen.Vortex.EnvironmentLightingService.Tests` 38/38. `git diff --check` passed with LF/CRLF warnings only. | `in_progress`; Oxygen now sanitizes local-fog authoring/packing ranges for densities, albedo, emissive, phase, and sort priority, and `vtx.local_fog.tile_max_instance_count` honors UE's `1..256` range. Focused tests cover authoring clamps and a cap-of-one Stage-14 local-fog path. | Not a UE5.7 local-fog parity closure. Shader code was not changed, so shader bake/catalog validation was not required for this slice. Remaining VTX-M04D.3 work: full source-to-target coverage, tiled/HZB culling proof, analytical integral and lighting proof, splat/compose and sky-depth exclusion proof, runtime/capture analyzer evidence, and VTX-M04D.4 local-fog volumetric injection after VTX-M04D.3 is sufficiently proven. |
| 2026-04-25 | VTX-M04D.4 Stage-15 fog integrated-scattering SRV payload proof | Changed tools/docs: `tools/vortex/AnalyzeRenderDocVortexBasicVolumetric.py`, `tools/vortex/Assert-VortexBasicVolumetricProof.ps1`, `tools/vortex/ProbeRenderDocEnvironmentStaticData.py`, `design/vortex/PLAN.md`, `design/vortex/IMPLEMENTATION_STATUS.md`, and `tools/vortex/README.md`. The focused volumetric RenderDoc analyzer read the then-current `EnvironmentStaticData` payload bound to the Stage-15 fog draw and validated `volumetric_fog.integrated_light_scattering_srv`, volumetric enabled/valid flags, and the captured grid dimensions. Syntax checks passed: `python -m py_compile tools/vortex/AnalyzeRenderDocVortexBasicVolumetric.py tools/vortex/ProbeRenderDocEnvironmentStaticData.py`. Focused verifier passed: `powershell -NoProfile -File tools/vortex/Verify-VortexBasicVolumetricProof.ps1 -CapturePath out/build-ninja/analysis/vortex/vortexbasic-proof-repair/full-pass2_capture.rdc -RuntimeLogPath out/build-ninja/analysis/vortex/vortexbasic-proof-repair/full-pass2.stderr.log -CaptureReportPath out/build-ninja/analysis/vortex/vortexbasic-proof-repair/full-pass2.volumetric-report.txt -ValidationReportPath out/build-ninja/analysis/vortex/vortexbasic-proof-repair/full-pass2.volumetric-validation.txt`; validation records `stage15_fog_environment_static_data_bound=true`, `stage15_fog_integrated_light_scattering_srv_valid=true`, `stage15_fog_integrated_scattering_valid_flag=true`, `stage15_fog_volumetric_enabled_flag=true`, `stage15_fog_volumetric_grid_valid=true`, `stage15_fog_integrated_light_scattering_srv=35826`, `stage15_fog_volumetric_flags=3`, `stage15_fog_volumetric_grid=240x180x32`, and `overall_verdict=pass`. One-off probe also passed against the same capture: `powershell -NoProfile -File tools/shadows/Invoke-RenderDocUiAnalysis.ps1 -CapturePath out/build-ninja/analysis/vortex/vortexbasic-proof-repair/full-pass2_capture.rdc -UiScriptPath tools/vortex/ProbeRenderDocEnvironmentStaticData.py -PassName VortexEnvironmentStaticDataProbe -ReportPath out/build-ninja/analysis/vortex/vortexbasic-proof-repair/full-pass2.environment-static-data.txt`. Current tooling now tracks the widened 672-byte payload and log-depth volumetric grid-z parameters. | `in_progress`; Stage-15 fog capture proof now validates the static bindless SRV payload and flags that drive the fog shader's integrated-scattering sample path. | Not a UE5.7 volumetric-fog parity closure. RenderDoc still reports `integrated_light_scattering_consumed_by_fog=false` through its dynamic read-only resource usage list, so that remains diagnostic; the accepted proof is captured static payload plus shader contract. Depth-aware froxel distribution, media/light/shadow/local-fog injection, temporal history/reprojection, and artifact-quality closure remain open. |
| 2026-04-25 | VortexBasic runtime proof repair for VTX-M04D environment validation | Changed code/tools: `Examples/VortexBasic/MainModule.cpp`, `Examples/VortexBasic/main_impl.cpp`, and `tools/vortex/Assert-VortexBasicRuntimeProof.ps1`. VortexBasic now authors the declared point and spot validation lights, moves them deterministically, and applies a startup-explicit `vtx.local_fog.enable=true` when `--with-local-fog` is requested so persisted user CVars cannot disable the proof path. The runtime assertion now treats cached atmosphere LUT dispatches, bindless consumption observations, and zero-delta Stage-15 atmosphere/local-fog visual samples as diagnostics instead of closure gates, while still requiring capture/runtime proof for local fog HZB use, integrated light scattering publication, Stage-12 point/spot/directional draws, Stage-14 local/volumetric dispatch, Stage-15 sky/fog/local-fog scopes, and final presentation. Build passed: `cmake --build --preset windows-debug --target oxygen-examples-vortexbasic`. Runtime smoke passed: `out\build-ninja\bin\Debug\Oxygen.Examples.VortexBasic.exe --frames 5 --fps 30 --with-atmosphere --with-height-fog --with-local-fog --with-volumetric-fog --capture-provider off` exited `0` and logged `CVar applied: name='vtx.local_fog.enable' value='true' origin=StartupExplicit`, ignored the lower-precedence persisted `false`, logged `local_fog_hzb_consumed=true`, `volumetric_fog_executed=true`, and `integrated_light_scattering_valid=true`. Full wrapper passed: `powershell -NoProfile -File tools/vortex/Run-VortexBasicRuntimeValidation.ps1 -Output out/build-ninja/analysis/vortex/vortexbasic-proof-repair/full-pass2 -Frame 5 -RunFrames 9 -Fps 30 -BuildJobs 4`; validation report `full-pass2.validation.txt` records `analysis_result=success`, `overall_verdict=pass`, `runtime_exit_code=0`, `stage12_spot_scope_count_match=true`, `stage12_point_scope_count_match=true`, `stage12_directional_scope_count_match=true`, `stage14_local_fog_scope_count_match=true`, `stage14_volumetric_fog_scope_count_match=true`, `stage15_fog_scope_count_match=true`, `stage15_local_fog_scope_count_match=true`, `local_fog_hzb_consumed=true`, `integrated_light_scattering_published=true`, and `final_present_nonzero=true`. | `in_progress`; the broader VortexBasic runtime wrapper now passes for the small validation scene and no longer has the stale Stage-12 spot-light blocker. | Not a city-scale visual proof or UE5.7 fog parity closure. Product analyzer diagnostics still report cached/missing per-frame transmittance, multi-scattering, and distant-sky LUT dispatches; bindless camera-aerial and integrated-scattering consumption remain unproven in RenderDoc; Stage-15 atmosphere/local-fog sampled color deltas remain zero in this proof. Full froxel/media/light/shadow/local-fog injection and history/reprojection parity remain open. |
| 2026-04-25 | VTX-M04D.4 focused VortexBasic volumetric RenderDoc proof tooling | Changed code/tools: `Examples/DemoShell/Runtime/DemoAppContext.h`, `Examples/VortexBasic/main_impl.cpp`, `Examples/VortexBasic/MainModule.cpp`, `src/Oxygen/Vortex/Environment/EnvironmentLightingService.cpp`, `tools/vortex/Run-VortexBasicRuntimeValidation.ps1`, `tools/vortex/AnalyzeRenderDocVortexBasicCapture.py`, `tools/vortex/AnalyzeRenderDocVortexBasicProducts.py`, `tools/vortex/Assert-VortexBasicRuntimeProof.ps1`, `tools/vortex/AnalyzeRenderDocVortexBasicVolumetric.py`, `tools/vortex/Assert-VortexBasicVolumetricProof.ps1`, and `tools/vortex/Verify-VortexBasicVolumetricProof.ps1`. Changed docs: `design/vortex/PLAN.md`, `design/vortex/IMPLEMENTATION_STATUS.md`, and `tools/vortex/README.md`. Build passed: `cmake --build --preset windows-debug --target oxygen-examples-vortexbasic Oxygen.Vortex.EnvironmentLightingService`. Syntax checks passed: `python -m py_compile tools/vortex/AnalyzeRenderDocVortexBasicVolumetric.py tools/vortex/AnalyzeRenderDocVortexBasicCapture.py tools/vortex/AnalyzeRenderDocVortexBasicProducts.py`. Tests passed: `ctest --preset test-debug -R "Oxygen.Vortex.EnvironmentLightingService" --output-on-failure` with 37/37 tests. Runtime smoke passed: `out\build-ninja\bin\Debug\Oxygen.Examples.VortexBasic.exe --frames 4 --fps 30 --with-atmosphere --with-height-fog --with-local-fog --with-volumetric-fog --capture-provider off` exited `0` and logged `volumetric_fog_executed=true`, `integrated_light_scattering_valid=true`, `volumetric_fog_grid=240x180x32`, and `volumetric_fog_dispatch=60x45x8` on active frames. Focused capture proof passed: `powershell -NoProfile -File tools/vortex/Verify-VortexBasicVolumetricProof.ps1 -CapturePath out/build-ninja/analysis/vortex/volumetric-runtime/vortexbasic-volumetric-frame5_capture.rdc -RuntimeLogPath out/build-ninja/analysis/vortex/volumetric-runtime/vortexbasic-volumetric-frame5.stderr.log -CaptureReportPath out/build-ninja/analysis/vortex/volumetric-runtime/vortexbasic-volumetric-frame5.volumetric-report.txt -ValidationReportPath out/build-ninja/analysis/vortex/volumetric-runtime/vortexbasic-volumetric-frame5.volumetric-validation.txt`; the validation report records `analysis_result=success`, `stage14_volumetric_fog_scope_present=true`, `stage14_volumetric_fog_dispatch_valid=true`, `stage15_fog_scope_present=true`, `stage15_fog_draw_valid=true`, `volumetric_fog_before_stage15_fog=true`, `integrated_light_scattering_written=true`, and `overall_verdict=pass`. Broader wrapper attempts at frames 3 and 5 remain failing before the new volumetric checks because the existing Stage-12 spot-light expectation is stale for the current VortexBasic action tree: `stage12_spot_scope_count mismatch: expected 1, got 0`. | `in_progress`; focused runtime/capture/analyzer evidence now exists for the first VortexBasic Stage-14 volumetric dispatch, named integrated-scattering product write, and later Stage-15 fog draw. | Not a UE5.7 volumetric-fog parity closure. RenderDoc still does not prove the Stage-15 bindless SRV read of `IntegratedLightScattering` (`integrated_light_scattering_consumed_by_fog=false` remains diagnostic), the broader VortexBasic proof wrapper needs the Stage-12 spot-light expectation corrected or re-scoped, and city-scale visual proof plus froxel depth distribution, media/light/shadow/local-fog injection, history/reprojection, and artifact-quality closure remain open. |
| 2026-04-25 | VTX-M04D.4 first volumetric integrated-scattering runtime path | Changed code: `src/Oxygen/Vortex/Environment/Passes/VolumetricFogPass.{h,cpp}`, `src/Oxygen/Graphics/Direct3D12/Shaders/Vortex/Services/Environment/VolumetricFog.hlsl`, `src/Oxygen/Vortex/Environment/EnvironmentLightingService.{h,cpp}`, `src/Oxygen/Vortex/Environment/Passes/FogPass.cpp`, `src/Oxygen/Vortex/SceneRenderer/SceneRenderer.{h,cpp}`, `src/Oxygen/Vortex/Types/EnvironmentFrameBindings.h`, `src/Oxygen/Vortex/Types/EnvironmentStaticData.h`, `src/Oxygen/Graphics/Direct3D12/Shaders/Vortex/Contracts/Environment/EnvironmentFrameBindings.hlsli`, `src/Oxygen/Graphics/Direct3D12/Shaders/Vortex/Contracts/Environment/EnvironmentStaticData.hlsli`, `src/Oxygen/Graphics/Direct3D12/Shaders/Vortex/Services/Environment/Fog.hlsl`, `src/Oxygen/Graphics/Direct3D12/Shaders/EngineShaderCatalog.h`, `src/Oxygen/Graphics/Direct3D12/Test/ShaderBakeCatalog_test.cpp`, `src/Oxygen/Vortex/CMakeLists.txt`, and `src/Oxygen/Vortex/Test/EnvironmentLightingService_test.cpp`. Changed docs: `design/vortex/PLAN.md`, `design/vortex/IMPLEMENTATION_STATUS.md`, and `design/vortex/lld/environment-service.md`. Vortex now allocates a `Vortex.Environment.IntegratedLightScattering` 3D texture when volumetric fog is authored and the view requests fog, dispatches a Stage-14 compute pass, publishes SRV/static binding validity, exposes SceneRenderer volumetric state, and composes the product in Stage 15 fog. Build passed: `cmake --build --preset windows-debug --target Oxygen.Vortex.EnvironmentLightingService`, `cmake --build --preset windows-debug --target oxygen-graphics-direct3d12`, `cmake --build --preset windows-debug --target Oxygen.Vortex.SceneRendererPublication`, and `cmake --build --preset windows-debug --target Oxygen.Examples.RenderScene.exe`. Tests passed: `ctest --preset test-debug -R "Oxygen.Vortex.EnvironmentLightingService" --output-on-failure` with 37/37 tests, `ctest --preset test-debug -R "Oxygen.Graphics.Direct3D12.ShaderBakeCatalog" --output-on-failure` with 4/4 tests, `ctest --preset test-debug -R "Oxygen.Vortex.SceneRendererPublication" --output-on-failure` with 16/16 tests, and the post-doc combined sweep `ctest --preset test-debug -R "Oxygen\.(Vortex\.(EnvironmentLightingService|SceneRendererPublication)|Graphics\.Direct3D12\.ShaderBakeCatalog)" --output-on-failure` with all 3 selected test binaries passing. ShaderBake through the Direct3D12 build reported `expanded_requests=183`, `dirty_requests=1`, `clean_requests=182`, `stale_requests=0`, and wrote 183 modules. Runtime smoke passed: `out\build-ninja\bin\Debug\Oxygen.Examples.RenderScene.exe --frames 3 --fps 30 --capture-provider=off` exited `0`. `git diff --check` passed with LF/CRLF warnings only. | `in_progress`; a first runtime integrated-scattering product path exists with focused service/publication/shader-catalog validation and bounded RenderScene startup/frame/shutdown evidence. | Not a UE5.7 volumetric-fog parity closure. Capture/analyzer proof was not run; depth-aware froxel distribution, temporal history/reprojection, shadowed/local/sky-light injection, local-fog injection, and artifact-quality proof remain open. Runtime smoke also logged one shutdown-time registered resource, `Vortex.DirectionalShadowSurface`, which was not investigated as part of this fog slice. |
| 2026-04-25 | VTX-M04D city-scale AP LUT resolution/depth default implementation | Changed code: `src/Oxygen/Vortex/Renderer.cpp`, `src/Oxygen/Vortex/Environment/Internal/AtmosphereLutCache.h`, `src/Oxygen/Vortex/Types/EnvironmentViewData.h`, `src/Oxygen/Graphics/Direct3D12/Shaders/Vortex/Contracts/Environment/EnvironmentViewData.hlsli`, and `src/Oxygen/Vortex/Test/EnvironmentLightingService_test.cpp`. Vortex's default camera aerial-perspective volume is now `64x64x32` over `96 km` instead of `32x32x16`, preserving CVar overrides while reducing city-scale screen/depth quantization; default depth slice length moves from `6 km` to `3 km`, with the first sqrt-mapped AP voxel center moving from about `94 m` to about `23 m`. Build passed: `cmake --build --preset windows-debug --target Oxygen.Vortex.EnvironmentLightingService Oxygen.Vortex.SceneRendererPublication Oxygen.Examples.RenderScene.exe`. Tests passed: `ctest --preset test-debug -R "Oxygen\.Vortex\.(EnvironmentLightingService\|SceneRendererPublication)" --output-on-failure` with EnvironmentLightingService 36/36 and SceneRendererPublication 16/16. Shader validation passed: `cmake --build --preset windows-debug --target oxygen-graphics-direct3d12`, direct `ShaderBake update` with `expanded_requests=182`, `dirty_requests=0`, `clean_requests=182`, `stale_requests=0`, and shader catalog tests 4/4. Runtime smoke passed: `out\build-ninja\bin\Debug\Oxygen.Examples.RenderScene.exe --frames 3 --fps 30 --capture-provider=off` exited `0`. | `in_progress`; AP LUT resolution/depth defaults now have focused implementation, tests, shader validation, and bounded RenderScene startup/frame/shutdown proof. | Capture/analyzer visual proof was not run, so city-scale banding/quality closure is not claimed. Volumetric-fog runtime behavior and artifact proof remain open. Cubemap fog remains deferred. |
| 2026-04-25 | VTX-M04D city-scale AP/fog plumbing implementation | Changed code: `Examples/DemoShell/DemoShell.{h,cpp}`, `Examples/DemoShell/Services/EnvironmentSettingsService.{h,cpp}`, `Examples/DemoShell/Test/EnvironmentSettingsService_test.cpp`, and `Examples/RenderScene/MainModule.cpp`. RenderScene now opts out of DemoShell's forced environment override so `Use Scene` can preserve scene-authored sky/atmosphere/fog settings, and the main view now requests local fog when the scene/settings service has enabled local fog volumes. Added `SceneAuthoredEnvironmentModeSyncsAtmosphereFogAndLocalFogRequests` coverage for scene-authored AP/fog/local-fog synchronization with override disabled. Build passed: `cmake --build --preset windows-debug --target Oxygen.Vortex.EnvironmentLightingService Oxygen.Vortex.SceneRendererPublication Oxygen.Examples.DemoShell.EnvironmentSettingsService.Tests Oxygen.Examples.RenderScene.exe`. Tests passed: `ctest --preset test-debug -R "Oxygen\.(Examples\.DemoShell\.EnvironmentSettingsService\|Vortex\.(EnvironmentLightingService\|SceneRendererPublication))" --output-on-failure` with DemoShell EnvironmentSettingsService 26/26, EnvironmentLightingService 36/36, and SceneRendererPublication 16/16. Runtime smoke passed: `out\build-ninja\bin\Debug\Oxygen.Examples.RenderScene.exe --frames 3 --fps 30 --capture-provider=off` exited `0`. Restricted source scan confirmed no remaining `kForceEnvironmentOverride` constant and showed the RenderScene override/local-fog hooks. `git diff --check` passed with only LF/CRLF warnings. | `in_progress`; implementation, focused validation, and bounded RenderScene startup/frame/shutdown evidence exist for DemoShell override behavior and local-fog request plumbing. | Superseded for AP LUT resolution/depth mapping by the following AP LUT default implementation row. Capture/analyzer proof, volumetric-fog runtime behavior, and visual artifact proof remain open. Cubemap fog remains deferred. |
| 2026-04-25 | VTX-M04D priority correction for city-scale AP/fog artifacts | Changed docs: `design/vortex/PLAN.md`, `design/vortex/IMPLEMENTATION_STATUS.md`, and `design/vortex/plan/VTX-M04D.2-exponential-height-fog-parity.md`. Corrected the planning/status language so height-fog cubemap inscattering is explicitly deferred and no longer presented as the next blocker. Redirected current priority toward `CityEnvironmentValidation` AP/fog artifact diagnosis: AP LUT resolution/depth mapping, DemoShell override behavior, local-fog request plumbing, volumetric-fog runtime behavior, and capture/analyzer proof. | `in_progress`; docs/status corrected. | No code or runtime validation in this docs-only correction. City-scale artifact remediation remains open; cubemap fog remains deferred. |
| 2026-04-25 | VTX-M04D.6 RenderScene visual validation and DemoShell AP control fix | Changed code: `Examples/DemoShell/Services/EnvironmentSettingsService.{h,cpp}`, `Examples/DemoShell/UI/EnvironmentDebugPanel.cpp`, `Examples/DemoShell/UI/EnvironmentVm.{h,cpp}`, `src/Oxygen/Graphics/Direct3D12/Shaders/Vortex/Services/Environment/AtmosphereCompose.hlsl`, `src/Oxygen/Vortex/Renderer.{h,cpp}`, `src/Oxygen/Vortex/Environment/Internal/AtmosphereLutCache.{h,cpp}`, `src/Oxygen/Vortex/Environment/EnvironmentLightingService.{h,cpp}`, `src/Oxygen/Vortex/Environment/Passes/AtmosphereCameraAerialPerspectivePass.{h,cpp}`, and focused tests. User visually confirmed AP in `Examples/RenderScene` after DemoShell stopped clamping UE's distance-scale-style AP control to `16`, the compose pass started using the main AP helper/gate, and the panel exposed effective AP enable/scale/start/strength/LUT controls. Build passed: `cmake --build --preset windows-debug --target Oxygen.Examples.DemoShell.EnvironmentSettingsService.Tests Oxygen.Vortex.EnvironmentLightingService Oxygen.Vortex.SceneRendererPublication Oxygen.Examples.RenderScene.exe`. Tests passed: `ctest --preset test-debug -R "Oxygen\.(Examples\.DemoShell\.EnvironmentSettingsService\|Vortex\.(EnvironmentLightingService\|SceneRendererPublication))" --output-on-failure` with DemoShell 25/25, EnvironmentLightingService 35/35, and SceneRendererPublication 16/16. ShaderBake ran during the build with `expanded_requests=182`, `dirty_requests=2`, `clean_requests=180`, `stale_requests=0`, and 182 modules written. Shader catalog tests passed: `ctest --preset test-debug -R "Oxygen.Graphics.Direct3D12.ShaderBakeCatalog" --output-on-failure` with 4/4 tests. `git diff --check` passed with only LF/CRLF warnings. | `in_progress`; RenderScene visual AP validation exists for the main scene path, with focused CPU/shader validation evidence. | Capture/analyzer proof and reflection/360-view AP runtime-resource proof remain missing. No local fog, volumetric fog, SkyLight capture/filtering, Async proof, or full environment runtime closure is claimed. |
| 2026-04-25 | VTX-M04D.6 aerial perspective implementation pass | Changed code: `src/Oxygen/Graphics/Direct3D12/Shaders/Vortex/Services/Environment/AerialPerspective.hlsli`, `src/Oxygen/Graphics/Direct3D12/Shaders/Vortex/Services/Environment/AtmosphereCameraAerialPerspective.hlsl`, `src/Oxygen/Vortex/Environment/Passes/AtmosphereCameraAerialPerspectivePass.{h,cpp}`, `src/Oxygen/Vortex/Environment/Internal/AtmosphereLutCache.{h,cpp}`, and `src/Oxygen/Vortex/Test/EnvironmentLightingService_test.cpp`. UE5.7 grounding checked earlier for `SkyAtmosphere.usf`, `SkyAtmosphereCommon.ush`, `BasePassPixelShader.usf`, `BasePassVertexShader.usf`, `SceneRendering.cpp`, and `SkyAtmosphereRendering.cpp`. Build passed: `cmake --build --preset windows-debug --target Oxygen.Vortex.EnvironmentLightingService Oxygen.Vortex.SceneRendererPublication`. Tests passed: `ctest --preset test-debug -R "Oxygen.Vortex.(EnvironmentLightingService\|SceneRendererPublication)" --output-on-failure` with 34/34 EnvironmentLightingService and 16/16 SceneRendererPublication tests. ShaderBake passed: `out\build-ninja\bin\Debug\Oxygen.Graphics.Direct3D12.ShaderBake.exe update --workspace-root F:\projects\DroidNet\projects\Oxygen.Engine --build-root F:\projects\DroidNet\projects\Oxygen.Engine\out\build-ninja\shaderbake --out F:\projects\DroidNet\projects\Oxygen.Engine\bin\Oxygen\Debug\dev\shaders.bin --mode dev`, with `expanded_requests=182`, `dirty_requests=19`, `clean_requests=163`, `stale_requests=0`, and 182 modules written. Shader catalog tests passed: `ctest --preset test-debug -R "Oxygen.Graphics.Direct3D12.ShaderBakeCatalog" --output-on-failure` with 4/4 tests. | `in_progress`; AP implementation and focused validation evidence exist, and M04D.1/M04D.2 publication/coupling tests were revalidated through the same focused suite. | Superseded by the following RenderScene visual validation row. Capture/analyzer proof and reflection/360-view AP runtime-resource proof remain missing. |
| 2026-04-25 | VTX-M04D.6 aerial perspective scope correction | Checked Vortex `AerialPerspective.hlsli` and restricted Vortex docs/status references for aerial perspective. Checked UE5.7 references for camera aerial-perspective generation, view publication, dummy/invalid resource behavior, and base-pass application: `SkyAtmosphere.usf`, `SkyAtmosphereCommon.ush`, `BasePassPixelShader.usf`, `BasePassVertexShader.usf`, `SceneRendering.cpp`, and SkyAtmosphere rendering references under UE5.7. Added detailed plan `VTX-M04D.6-aerial-perspective-parity.md`, wired the milestone into `PLAN.md`, and updated this status ledger. Restricted markdown scans for VTX-M04D.6 visibility and AP parity wording passed; `git diff --check` passed. | `planned`; AP was explicitly not validated for UE5.7 parity at scope-correction time. | Superseded by later implementation and RenderScene visual-validation rows. Capture/analyzer proof and reflection/360-view runtime-resource proof remain missing before any AP parity or full atmosphere runtime closure claim. |
| 2026-04-25 | VTX-M04D.2 exponential height fog implementation and visual cleanup | Changed code: `src/Oxygen/Vortex/Types/EnvironmentStaticData.h`, `src/Oxygen/Vortex/Environment/EnvironmentLightingService.cpp`, `src/Oxygen/Vortex/Environment/Passes/FogPass.cpp`, `src/Oxygen/Graphics/Direct3D12/Shaders/Vortex/Contracts/Environment/EnvironmentStaticData.hlsli`, `src/Oxygen/Graphics/Direct3D12/Shaders/Vortex/Services/Environment/Fog.hlsl`, `src/Oxygen/Graphics/Direct3D12/Shaders/Vortex/Services/Environment/AerialPerspective.hlsli`, `src/Oxygen/Scene/Environment/Fog.h`, `src/Oxygen/Vortex/Environment/Types/HeightFogModel.h`, `src/Oxygen/Data/PakFormat_world.h`, `Examples/DemoShell/*`, and `Examples/RenderScene/*`. Vortex now publishes a widened height-fog payload, uses UE5.7-shaped analytic fog in the fog pass, excludes far-depth sky pixels, removes the old simplified aerial-perspective fog fallback, aligns meter-space defaults with UE's `/1000` centimeter-space density/falloff scaling, and exposes supported parameters through DemoShell/RenderScene. UE5.7 grounding checked: `Renderer\Private\FogRendering.cpp`, `Shaders\Private\HeightFogCommon.ush`, and `Engine\Private\Components\ExponentialHeightFogComponent.cpp`. Build passed: `cmake --build --preset windows-debug --target Oxygen.Vortex.EnvironmentLightingService Oxygen.Vortex.SceneRendererPublication Oxygen.Examples.DemoShell.EnvironmentSettingsService.Tests Oxygen.Examples.RenderScene.exe`. Tests passed: `ctest --preset test-debug -R "Oxygen.Vortex.(EnvironmentLightingService\|SceneRendererPublication)\|Oxygen.Examples.DemoShell.EnvironmentSettingsService" --output-on-failure` with 32/32 EnvironmentLightingService, 16/16 SceneRendererPublication, and 24/24 DemoShell EnvironmentSettingsService tests. ShaderBake/catalog passed: `cmake --build --preset windows-debug --target oxygen-graphics-direct3d12` and direct `ShaderBake update`, with `expanded_requests=182`, `dirty_requests=0`, `clean_requests=182`, `stale_requests=0`. Restricted source/doc scans found no remaining temporary M04D.2 normalized-ray or fallback claims, and `git diff --check` passed. | `in_progress`; implementation, visual confirmation, and fresh validation evidence exist, but VTX-M04D.2 is not marked validated because runtime/capture proof and city-scale artifact remediation remain open. | Cubemap inscattering authoring is represented in the GPU contract and tests prove authored-but-unusable publication; runtime cubemap resource binding/sampling remains explicitly deferred. Current follow-up is city-scale AP/fog artifact remediation and proof. No local fog, volumetric fog, SkyLight capture/filtering, Async proof, or full environment runtime closure is claimed. |
| 2026-04-25 | VTX-M04D.1 environment publication truth implementation | Changed code: `src/Oxygen/Vortex/Environment/EnvironmentLightingService.{h,cpp}`, `src/Oxygen/Vortex/Environment/Passes/IblProbePass.cpp`, `src/Oxygen/Vortex/Environment/Types/EnvironmentProbeState.h`, `src/Oxygen/Vortex/Environment/Types/EnvironmentViewProducts.h`, `src/Oxygen/Vortex/SceneRenderer/SceneRenderer.{h,cpp}`, `src/Oxygen/Vortex/Types/EnvironmentFrameBindings.h`, `src/Oxygen/Vortex/Types/EnvironmentStaticData.h`. Changed tests: `src/Oxygen/Vortex/Test/EnvironmentLightingService_test.cpp`, `src/Oxygen/Vortex/Test/SceneRendererPublication_test.cpp`. Build: `cmake --build --preset windows-debug --target Oxygen.Vortex.EnvironmentLightingService Oxygen.Vortex.SceneRendererPublication` passed. Tests: `ctest --preset test-debug -R "Oxygen.Vortex.(EnvironmentLightingService\|SceneRendererPublication)" --output-on-failure` passed with `Oxygen.Vortex.EnvironmentLightingService.Tests` 28/28 and `Oxygen.Vortex.SceneRendererPublication.Tests` 16/16. CPU/HLSL ABI: no shader files or struct sizes changed; no shader bake/catalog validation required. UE5.7 grounding: retained the `environment-service.md` reference families for SkyAtmosphere, FogRendering, LocalFogVolumeRendering, VolumetricFog, and SkyLight capture/filtering contracts; no full fog parity review was performed because VTX-M04D.2 through VTX-M04D.4 remain out of scope. | `validated` for VTX-M04D.1 publication truth only. | Real SkyLight capture/filtering resources are still not generated and are explicitly published unavailable; height fog, local fog, and volumetric fog parity/runtime output remain open in VTX-M04D.2 through VTX-M04D.4. |
| 2026-04-25 | VTX-M04D.1 detailed planning | Added the detailed environment publication truth plan at [plan/VTX-M04D.1-environment-publication-truth.md](./plan/VTX-M04D.1-environment-publication-truth.md), the planning package index at [plan/README.md](./plan/README.md), and the reusable workflow at [plan/milestone-planning-workflow.md](./plan/milestone-planning-workflow.md). | Historical planning handoff, superseded by the validated implementation evidence row above. | No remaining VTX-M04D.1 planning-only gap; runtime fog parity remains tracked by later VTX-M04D work packages. |
| 2026-04-25 | Planning/status rewrite | Source/doc inspection; three read-only subagent reviews; restricted markdown scans for stale status links, next milestone visibility, environment/fog false-completion language, and milestone status consistency; `git diff --check` on edited docs. | `validated` for VTX-M00 docs-only planning/status work. | Existing implementation remains `landed_needs_validation` or `in_progress` until fresh build/test/runtime proof is recorded. |
