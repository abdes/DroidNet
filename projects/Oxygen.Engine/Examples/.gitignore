# Ignore demo settings generated by example runs (top-level and in subfolders)
demo_settings.json
**/demo_settings.json

# Explicit user-authored LightBench snapshot (shipped indoor example is tracked).
LightBench/lightbench.saved.json
