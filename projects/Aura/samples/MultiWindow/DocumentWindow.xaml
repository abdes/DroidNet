<?xml version="1.0" encoding="utf-8" ?>

<Window
    x:Class="DroidNet.Samples.Aura.MultiWindow.DocumentWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <Grid.Background>
            <SolidColorBrush Opacity="0.7" Color="{ThemeResource SolidBackgroundFillColorBase}" />
        </Grid.Background>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!--  Document Toolbar  -->
        <Border
            Grid.Row="0"
            Padding="12"
            BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
            BorderThickness="0,0,0,1">
            <Border.Background>
                <SolidColorBrush Opacity="0.8" Color="{ThemeResource CardBackgroundFillColorDefault}" />
            </Border.Background>
            <StackPanel Orientation="Horizontal" Spacing="8">
                <AppBarButton Icon="Save" Label="Save" />
                <AppBarButton Icon="Undo" Label="Undo" />
                <AppBarButton Icon="Redo" Label="Redo" />
                <AppBarSeparator />
                <AppBarButton Icon="Bold" Label="Bold" />
                <AppBarButton Icon="Italic" Label="Italic" />
                <AppBarButton Icon="Underline" Label="Underline" />
            </StackPanel>
        </Border>

        <!--  Document Content  -->
        <Grid Grid.Row="1" Padding="20">
            <StackPanel
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                Spacing="16">
                <FontIcon
                    FontSize="64"
                    Foreground="{ThemeResource AccentFillColorDefaultBrush}"
                    Glyph="&#xE8A5;" />
                <TextBlock
                    FontSize="24"
                    FontWeight="Bold"
                    Text="Document Window"
                    TextAlignment="Center" />
                <TextBlock
                    Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                    Text="A content editing window with toolbar"
                    TextAlignment="Center" />
                <Border
                    Margin="0,16,0,16"
                    Padding="12,8"
                    Background="{ThemeResource AccentFillColorDefaultBrush}"
                    CornerRadius="4">
                    <TextBlock
                        HorizontalAlignment="Center"
                        FontSize="12"
                        Foreground="White"
                        Text="💡 The backdrop effect is visible through the semi-transparent background" />
                </Border>
                <RichEditBox
                    Width="600"
                    Height="400"
                    PlaceholderText="Start typing your document..." />
            </StackPanel>
        </Grid>
    </Grid>
</Window>
