<?xml version="1.0" encoding="utf-8" ?>

<UserControl
    x:Class="DroidNet.Docking.Demo.Controls.DockableInfoView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <StackPanel
        Margin="0,10"
        Padding="5"
        BorderBrush="Blue"
        BorderThickness="1"
        Orientation="Vertical"
        Spacing="10">
        <TextBlock TextWrapping="Wrap"><Run Text="Dockable Id: " /><Run Text="{x:Bind ViewModel.DockableId}" /></TextBlock>
        <TextBlock TextWrapping="Wrap"><Run Text="Onwer Dock Id: " /><Run Text="{x:Bind ViewModel.DockId}" /></TextBlock>
        <TextBlock TextWrapping="Wrap"><Run Text="Layout Group: " /><Run Text="{x:Bind ViewModel.GroupInfo}" /></TextBlock>
    </StackPanel>
</UserControl>
